# 引き継ぎ記録

オーケストレーターだけが書く。**新しいセッションはここから読む。**

---

## ゴール

> 要件定義書のうち **MVP01** の実装と、**実投稿を除く**テストの完了。

### 制約（PO 指定・2026-08-23）

| # | 制約 |
| --- | --- |
| C-1 | X への実際の投稿を行わない |
| C-2 | 認証情報は環境に置かれていない。取得しようとしない |
| C-3 | 投稿処理は、ダミーの送信先か、記録するだけの実装に対して開発する |
| C-4 | 実際の投稿を試す必要が出たら、`docs/po-queue.md` へ登録して止まる |

### ゴール達成条件

**未確定。要件定義書が未受領のため。**

要件定義書を受領した時点で、オーケストレーターが下記4点を確定させ、この節を
書き換えてからタスク分解に入る。**確定前にタスクを登録・割り当てしない。**

| # | 確定させる事項 | 現状 |
| --- | --- | --- |
| G-1 | MVP01 に含まれる機能の一覧（MVP02以降との境界） | 要件定義書待ち |
| G-2 | 「実投稿以外のテスト完了」の到達点。投稿経路の実装形態と一体で決まる | 要件定義書待ち |
| G-3 | 技術スタック（言語・ランタイム・テストフレームワーク）と、その決定主体 | 要件定義書待ち。CLAUDE.md では未確定のまま |
| G-4 | 完了と報告してよい条件（テスト合格 / レビュー通過 / PO の実行確認まで要るか） | 要件定義書待ち |
| G-5 | 認証情報の読み込み口を MVP01 の範囲に含めるか | 要件定義書待ち |

G-2 について、2026-08-23 時点で想定される分岐（要件定義書を読んだうえで PO と確定する）:

- **A** X API クライアントを本実装し、送信先を差し替え可能にする。ローカルのダミー HTTP サーバへ送って結合テストまで通す
- **B** 「記録するだけ」の実装のみ。HTTP は一切出さず、投稿インターフェースのモックで受け入れテストを判定する
- **C** A と B の両方を用意し、既定は B、結合テストのときだけ A

---

## セッション記録

### 2026-08-23 セッション1（ブートストラップ）

やったこと:

- 台帳を作成した（`docs/task-list-media-agent.csv` / `docs/tasks/README.md` / `docs/po-queue.md` / 本ファイル）
- ゴール達成条件の確定に必要な4点（G-2〜G-5）を PO へ照会した
- PO 回答: **「まず要件定義書を見てから話そう」**。4点とも保留。`docs/po-queue.md` へは登録していない
  （登録の要件である「待つ間も進められるタスク」が書けない — タスクが0件で、組織全体が
  要件定義書の受領を待っている状態のため。この事実自体が報告に値する）

やっていないこと:

- タスク分解・登録・割り当て（要件定義書が未受領のため着手不可）
- ゴール健全性評価（タスク0件。前回比が取れない）

---

## 次のセッションが最初にやること

1. 要件定義書が受領済みかを確認する。**未受領ならタスク分解に入らない**
2. 受領済みなら、上表 G-1〜G-5 を PO と確定させ、「ゴール達成条件」の節を書き換える
3. `docs/po-queue.md` の未回答を確認する
4. `python3 .claude/scripts/org-check.py` を実行する
