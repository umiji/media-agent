# T-008: Stage 0 受け入れ・回帰テスト実行

## メタデータ
- 作成日: 2026-08-23
- 更新日: 2026-08-24
- 状態: 完了
- 優先度: 高
- 担当: org-test
- 進捗: 完了。判定は合格（Blocker / Major は0件）。538 passed、A-2 を org-test 自身が実環境で再現した
- 依存: T-007（ブロッカー）— Stage 0 の実装が全て揃っていないと、受け入れ判定ができないため
- TDD適用可否: 適用外（このタスクは既存テストの実行と判定であり、新たに落ちるテストを先に書く工程ではない）
- 手戻り回数: 0

## 目的

Stage 0 が**仕様の側から見て**完成しているかを判定する。実装エージェントの自己申告ではなく、
独立した実行と観測によって、ゴール達成条件 A-2 と A-4 の充足を確定させる。

要件定義書25節は「各Stageは、実装完了ではなく実運用上の検証完了をもって完了とする」としている。
**このタスクがその検証にあたる。**

## 変更範囲

- 受け入れテストの実行と、結果の記録
- 不足しているシナリオテスト・回帰テストの**追加**（T-003 で書いたものに足りない観点があれば）
- 本ファイル `docs/tasks/T-008.md` の 決定ログ / 作業ログ / 証拠 / 進捗

## 禁止事項

- **製品コードを修正しない。** 不具合を見つけたら org-implementation へ不具合レポートを返す（2往復まで）。
  2往復で解決しなければ、解決していなくてもオーケストレーターへ返す
- **テストの期待値を実装に合わせて書き換えない。** 期待値が誤っていると判断したら、
  設計成果物と突き合わせたうえで報告する
- 設計成果物（`docs/design/` 配下）を書き換えない
- `docs/task-list-media-agent.csv` / `docs/handover.md` / `docs/po-queue.md` / 他タスクの `T-XXX.md` を書き換えない
- 単体テストを書かない（実装エージェントの担当）
- **ネットワークへ出るテスト・認証情報を要求するテストを書かない**（PO 制約 C-1・C-2）

## 完了条件

1. **受け入れテスト S-A 〜 S-I を実行し、全通過している。** 実行コマンドと実出力を `## 証拠` へ記録する
2. **実環境で、要件定義書18節 Stage 0 の検証条件を人手で再現している。**
   一時ディレクトリで `media-agent init` → `media-agent doctor` → `media-agent status` を実際に実行し、
   **各コマンドの実出力と終了コードを `## 証拠` へ貼る**。ゴール達成条件 A-2 の根拠となる
3. 単体テストを含む**全テストが通っている**（実行コマンドと実出力を記録）
4. 次のシナリオテストが存在し、通っている（T-003 に無ければ追加する）
   - **S-J** 別々の2ディレクトリで `init` したとき、DB・ログ・Memory が**互いに混ざらない**
     （要件定義書5.4節「Memoryは他プロジェクトと完全分離する」）
   - **S-K** 同じディレクトリで `init` → `doctor` → `status` → `run` → `status` と続けて実行しても壊れない
5. **Stage 1 以降の機能が実装されていないことを確認している**（ゴール達成条件 A-8）。
   具体的には、リポジトリ内に X API クライアント・外部 HTTP 送信・LLM 呼び出しのコードが**無い**ことを
   検索で確認し、確認に使ったコマンドと結果を `## 証拠` へ記録する
6. 不具合を検出した場合、**再現手順・原因の切り分け・該当箇所**を添えて org-implementation へ返し、
   その往復の結果が `## 作業ログ` に記録されている

### T-007 からの接続点（2026-08-24・オーケストレーター追記）

T-007 は完了済み（コミット `2f12cbd` / `52b98ee`、記録 `e1f64d1`）。**Stage 0 の実装は全て揃った。**

| # | 内容 |
| --- | --- |
| N-1 | 着手時点で `PATH="$PWD/.venv/bin:$PATH" .venv/bin/pytest -q` が **522 passed**（単体404 / 受け入れ118）。オーケストレーターが実地で再現済み。**この値からの差分が回帰である** |
| N-2 | **`.venv/bin` を PATH に置かずに実行すると console script の疎通テスト1件が落ちる。** `CLAUDE.md` 既記載の環境条件であり不具合ではない。**これを不具合として報告しないこと** |
| N-3 | **GitHub Actions の実際の実行は未検証である**（T-001 の持ち越し X-2）。workflow は `.github/workflows/ci.yml` に存在し、ファイルとしての妥当性までは T-007 が確認した。**push 済みの CI 実行結果の観測が本タスクの起点になる。** Python 3.12 / 3.13 での実行もこの環境では未確認 |
| N-4 | **`media-agent run --agent fail` は stderr にトレースバックを出す。** `core/runtime/runner.py:166` の `logger.error(..., exc_info=exc)` と詳細設計 11.2 の組み合わせによるもので、**設計どおりである**。T-007 から「利用者体験としてはノイズで、`app.py` の方針と非対称」との申告があり、**T-009 のレビュー観点に入れた。不具合として扱わないこと** |
| N-5 | `tests/fixtures/`（方式設計 10.1）は**作られていない**。必要なテストデータが無く、空ディレクトリが「実装済み」に見えるのを避けたため。**本タスクでテストデータが要るなら、ここで作ってよい** |
| N-6 | T-007 が設計に無い箇所を自分で決めた事項が6件ある（`docs/tasks/T-007.md` の決定ログ）。**T-009 のレビュー対象**であり、本タスクは前提として扱ってよい |

## 判断してよい範囲

- テストの追加（Stage 0 の範囲内であれば）
- 不具合の原因切り分けの深さ
- org-implementation への不具合レポートの粒度（2往復の上限内で）

## テスト方法

このタスク自体がテスト工程である。

## 停止条件

共通の停止条件に加えて:

- 受け入れテストが通らず、**org-implementation との2往復でも解決しない**
- 受け入れテストの期待値と設計成果物と実装の三者が食い違っており、どれが正しいか判断できない
- 完了条件5で、**X API クライアントまたは外部送信のコードが見つかった**（PO 制約違反。直ちに報告する）

## 参照すべき成果物

| パス | 何が書いてあるか |
| --- | --- |
| `docs/requirements-media-agent-v0.2.md` | 要件定義書 v0.2 全文。**18節 Stage 0・19節の対応表・28節を必ず読むこと** |
| `docs/design/stage0-architecture.md` | T-001 の方式設計成果物（構造・技術選定・実行コマンド） |
| `docs/design/stage0-detail.md` | T-002 の詳細設計成果物（Config / DB / Agent Interface / Task / Policy / Logging・Audit） |
| `docs/handover.md` | 今回のゴール達成条件 A-1〜A-8、PO 制約 C-1〜C-4 |
| `CLAUDE.md` | AI開発組織の運営規約 |
| `.claude/rules/org-task-ledger.md` | 決定ログ / 作業ログ / 証拠 の書き方 |
| `.claude/rules/org-escalation.md` | 共通の停止条件とエスカレーション手順 |
| `docs/glossary.md` | この組織で意味が決まっている語 |

**PO 制約（今回のゴール全体に効く）**:

- C-1 X への実際の投稿を行わない
- C-2 認証情報は環境に置かれていない。取得しようとしない
- C-3 投稿処理は、ダミーの送信先か、記録するだけの実装に対して開発する
- C-4 実際の投稿を試す必要が出たら、作業を止めてオーケストレーターへ報告する

**Stage 0 の範囲では投稿処理そのものが存在しない。** 要件定義書28節が「Stage 0 ではまだ X API や
ニュース取得等を実装しない」と明記しているため、ダミー送信先すら作らない。

なお、`docs/design/` 配下の実際のファイル名は T-001 / T-002 の成果物に従うこと。上表は想定である。

## 成果物

| 種別 | 実体 |
| --- | --- |
| S-J（プロジェクト分離・10ケース） | `tests/acceptance/test_stage0_project_isolation.py`（コミット `b2f1fc7`） |
| S-K（連続実行・6ケース） | `tests/acceptance/test_stage0_lifecycle_sequence.py`（コミット `b2f1fc7`） |
| 判定と証拠 | 本ファイルの `## 証拠` |

**製品コードは1行も変更していない**（禁止事項どおり）。

## ブロッカー

無し。

## 決定ログ

### 2026-08-24 S-J にプロジェクトルート解決の2ケースを含めた
- 決定: S-J（分離）へ、入れ子プロジェクトの上方探索（方式設計 6.3 の 2）と
  `MEDIA_AGENT_PROJECT_DIR` より `-C` が優先されること（方式設計 6.2）の2ケースを含めた
- 却下案: 完了条件4 の文言どおり「2ディレクトリでの分離」だけを書く → **分離が壊れる最大の経路が
  パス解決の誤りである**。設計に書かれた解決規則が誤ると、利用者は「別プロジェクトへ書き込んだこと」に
  気付けない。T-003 の S-A〜S-I にはこの2つを判定するケースが無かった（`init` の非探索のみ）
- 付帯条件: 上方探索の検証は `-C` を渡さない挙動そのものが対象のため、この1ケースだけ
  `monkeypatch.chdir` を使う。移動先は `tmp_path` 配下に限る（罠 T-3）。
  `tests/conftest.py` の「`os.chdir` は使わない」という T-003 の申し合わせからの逸脱であり、
  逸脱であることをテストの docstring に明記した
- 出典: セッション内の判断。根拠は `docs/design/stage0-architecture.md` 6.2・6.3

### 2026-08-24 Python 3.12 / 3.13 の検証を、CI の観測の代替として実施した
- 決定: GitHub Actions の実行結果を観測できなかったため、CI の matrix と同じ手順
  （fresh venv → `pip install -e ".[dev]"` → ruff check → ruff format --check → mypy src → pytest）を
  **ローカルの Python 3.12.3 / 3.13.12 で実地に再現した**
- 却下案: 「観測できなかった」とだけ書いて終える → 持ち越し X-2 の実質は
  「宣言した 3.11/3.12/3.13 で本当に動くか」であり、その部分はこの環境で確かめられる。
  確かめられる部分を確かめずに未検証として送ると、T-009 と PO が同じ問いを繰り返す
- 付帯条件: **これは GitHub Actions そのものの観測ではない。** actions/checkout・setup-python・
  pip キャッシュ・ubuntu-latest 上での実行は依然として未検証である。X-2 は解消していない
- 出典: セッション内の判断

### 2026-08-24 検出した2件を不具合レポートとして org-implementation へ返さなかった
- 決定: F-1（構造が壊れたプロジェクトでの終了コード 70）と F-2（受け入れテスト側の接続リーク）を、
  org-implementation への不具合レポートにせず、オーケストレーターへの報告に留めた。往復は0回
- 却下案: F-1 を不具合として実装へ返す → **設計（詳細設計 17.2 のコマンド × 状況の一覧）が
  この状況を定めていない。** 定めのない期待値をテストが作って実装へ要求すると、
  誰にも照合されない契約が生まれる（TDD の構造上の弱点そのもの）。
  「実装は設計どおりだが期待動作にならない」＝設計側の問題としてオーケストレーターへ上げるのが正しい
- 却下案: F-2 を自分で直す → `tests/acceptance/expectations.py` の修正は T-008 の変更範囲
  （実行・記録・シナリオテストの追加・本ファイル）に無い
- 出典: セッション内の判断。`CLAUDE.md`「スコープを勝手に広げない」/ 本ロールの「テストが落ちたときの行き先」

## 作業ログ

1. 着手前確認: `git status` クリーン・HEAD `a92b24b`。T-008.md の必須項目（完了条件 / 判断してよい範囲 /
   禁止事項 / 停止条件 / 参照すべき成果物）が揃っていることを確認し、差し戻しは不要と判断した
2. 基準値の再現: `pytest -q` → **522 passed**。`ruff check` / `ruff format --check` / `mypy src` すべて 0。
   申し送り N-1 の値と一致
3. 受け入れテスト S-A〜S-I をファイル単位で実行し、内訳を確定させた（完了条件1）
4. **A-2 を一時ディレクトリで実環境再現した**（完了条件2）。`init` / `doctor` / `status` すべて終了コード 0
5. S-J / S-K を作成した（完了条件4）。16ケース追加、`522 → 538 passed`。コミット `b2f1fc7`
6. A-8 の不在確認を git 管理下の全コード・設定97ファイルへ対して実施した（完了条件5）
7. GitHub Actions の観測を試みたが**できなかった**。代替として Python 3.12 / 3.13 で CI と同じ手順を実地再現した
8. 追加の探索的検証（テスト基盤の健全性・製品コードの外部接続不在・壊れたプロジェクトでの挙動）を行い、
   Minor 2件を検出した。**org-implementation との往復は行っていない**（理由は決定ログ）

## 証拠

### E-1 完了条件1: 受け入れテスト S-A〜S-I（+ 追加分）が全通過

```
$ PATH="$PWD/.venv/bin:$PATH" .venv/bin/pytest -q tests/acceptance
```

| ID | シナリオ | ファイル | 件数 | 結果 |
| --- | --- | --- | --- | --- |
| S-A | `init` によるプロジェクト生成 | `test_stage0_init.py` | 8 | passed |
| S-B | `doctor` の検証 | `test_stage0_doctor.py` | 7 | passed |
| S-C | `status` の出力 | `test_stage0_status.py` | 6 | passed |
| S-D | 未初期化とスタブコマンド | `test_stage0_uninitialized.py` | 20 | passed |
| S-E | Agent Runtime | `test_stage0_agent_runtime.py` | 11 | passed |
| S-F | Task の状態遷移 | `test_stage0_task_lifecycle.py` | 14 | passed |
| S-G | Policy Engine の判定 | `test_stage0_policy.py` | 14 | passed |
| S-H | Audit の9項目 | `test_stage0_audit.py` | 7 | passed |
| S-I | `config.yaml` 異常系 | `test_stage0_config_errors.py` | 26 | passed |
| （S-A〜S-I 外） | entry point 疎通 | `test_stage0_entrypoint.py` | 2 | passed |
| （S-A〜S-I 外） | ネットワーク遮断の自己検査 | `test_network_guard.py` | 3 | passed |
| **S-J**（本タスクで追加） | プロジェクト分離 | `test_stage0_project_isolation.py` | 10 | passed |
| **S-K**（本タスクで追加） | 連続実行 | `test_stage0_lifecycle_sequence.py` | 6 | passed |
| | | **合計** | **134** | **0 failed** |

### E-2 完了条件2: A-2 の実環境再現（org-test が独立に実行した実出力）

一時ディレクトリ `.../scratchpad/a2/demo-project` にて、`.venv/bin` を PATH に置いた状態で実行した。

```
$ media-agent init
created  .media-agent/config.yaml
created  .media-agent/strategy.md
created  .media-agent/rules.md
created  .media-agent/agents/.gitkeep
created  .media-agent/memory/.gitkeep
created  .media-agent/data/
created  .media-agent/logs/
created  .media-agent/.gitignore
created  .media-agent/data/media-agent.db
Media Agent を初期化しました: .../scratchpad/a2/demo-project
EXIT=0

$ media-agent doctor
Media Agent doctor — .../scratchpad/a2/demo-project
[ok]      structure.files           config.yaml / strategy.md / rules.md がそろっています
[ok]      structure.dirs            agents / memory / data / logs がそろっています
[ok]      config.syntax             config.yaml を YAML として読めます
[ok]      config.schema             設定はスキーマを満たしています
[ok]      config.version            config 版数 1
[ok]      config.consistency        content.posts_per_day (3) <= actions.post.max_per_day (3)
[ok]      db.file                   media-agent.db を SQLite として開けます
[ok]      db.schema                 スキーマ版数 1 / 6 テーブルがそろっています
[ok]      db.foreign_keys           外部キー制約が有効です
[ok]      logs.writable             .media-agent/logs へ書き込めます
[ok]      security.gitignore        data/, logs/, .env が除外されています
[skipped] security.env_not_tracked  .env はありません（Stage 0 では認証情報を使いません）
[ok]      agents.registry           2 件の Agent が登録されています: echo, fail
[ok]      policy.config             4 件の Action を判定できます: post=allow reply=require_approval repost=require_approval like=deny
[ok]      runtime.python            Python 3.11
結果: 14 ok / 0 warn / 1 skipped / 0 fail
EXIT=0

$ media-agent status
Media Agent status — .../scratchpad/a2/demo-project
project      : demo-project
config       : version 1 / platform x / posts_per_day 3
automation   : require_approval=false  post=auto reply=approval repost=approval like=disabled
database     : .media-agent/data/media-agent.db (schema 1)
agents       : 2 registered — echo, fail
tasks        : total 0 — pending 0 / running 0 / completed 0 / failed 0 / cancelled 0
recent tasks :
  (タスクはありません)
EXIT=0
```

`init` の生成物は要件定義書8.1節・詳細設計 12.1 の8項目 +DB がそろっている。
`doctor` は `fail` 0件、`security.env_not_tracked` が `[skipped]`（認証情報の不在を異常としない・PO 制約 C-2）。

**インストール済み環境での再現（罠 D-T17）**: 別の fresh venv へ `pip install /home/user/media-agent`
（**editable ではない**）を行い、同じ3コマンドを実行した。すべて終了コード 0 で、
`config.yaml` / `strategy.md`（590B）/ `rules.md`（735B）/ `.gitignore`（187B）が
wheel から正しく展開された。**テンプレートのパッケージデータ漏れは無い。**

### E-3 完了条件3: 全テストが通っている

```
$ PATH="$PWD/.venv/bin:$PATH" .venv/bin/pytest -q
538 passed in 10.92s          # EXIT=0（単体 404 / 受け入れ 134）
$ .venv/bin/ruff check .      # EXIT=0
$ .venv/bin/ruff format --check .  # EXIT=0
$ .venv/bin/mypy src          # EXIT=0
```

順序への非依存も確認した（`pytest tests/acceptance tests/unit` の順でも 538 passed、
連続3回すべて 538 passed）。着手時の基準値 522 に対し **+16 は本タスクで追加した S-J / S-K であり、回帰は0件**。

### E-4 完了条件4: S-J / S-K

**S-J**（`tests/acceptance/test_stage0_project_isolation.py`・要件定義書5.4節）

| 判定するもの |
| --- |
| 2つの `.media-agent/` が別の実体である |
| `status` が報告する名前とルートが対象プロジェクトのものである（詳細設計 12.3） |
| 片方で実行した Task が、もう片方の DB に存在しない |
| `task list` の task_id 集合が互いに素である |
| `status` の集計がもう一方の Task を数えない |
| 監査記録が `decisions`（正本）と `audit.jsonl`（写し）の両方で分離している |
| **同一プロセスで続けて実行しても、運用ログのハンドラが張り替わり前のプロジェクトへ書かれない** |
| 片方の `config.yaml` を壊しても、もう片方が健全なままである |
| **入れ子プロジェクトで上方探索が「近い方」を掴む**（方式設計 6.3 の 2） |
| **`MEDIA_AGENT_PROJECT_DIR` は効くが `-C` のほうが強い**（方式設計 6.2） |

**S-K**（`tests/acceptance/test_stage0_lifecycle_sequence.py`・要件定義書18節）

| 判定するもの |
| --- |
| `init` → `doctor` → `status` → `run` → `status` の各段が終了コード 0 で、`status` の集計が 0 → 1 に変わる |
| `run` の副作用で `doctor` の検査結果が変化しない |
| **2周目の `init` が既存の Task 行を消さない**（DB の冪等性。S-A はテンプレートしか見ていない） |
| `fail` Agent の実行後も後続コマンドが動き、`running` のまま残る Task が無い |
| 読み取り系を挟んで3回繰り返しても、集計・task_id 数・`decisions` 行数が一致し続ける |
| 人間向け `status` の出力も一連の流れを通して壊れない |

### E-5 完了条件5: Stage 1 以降の機能が実装されていない（A-8）

git 管理下の `*.py` / `*.toml` / `*.yml` / `*.yaml` / `*.cfg` **97ファイル**を対象に検索した。

```
$ PYFILES=$(git ls-files '*.py' '*.toml' '*.yml' '*.yaml' '*.cfg')
$ grep -nE "\b(requests|httpx|aiohttp|urllib3|urllib\.request|http\.client|xmlrpc|ftplib|smtplib|telnetlib)\b" $PYFILES
(no match)
$ grep -niE "\b(tweepy|python-twitter|twitter|twikit|x_api|xapi)\b" $PYFILES
(no match)
$ grep -niE "\b(openai|anthropic|google-generativeai|langchain|llama_index|litellm|transformers|ollama)\b" $PYFILES
(no match)
$ grep -nE "https?://" $PYFILES
tests/unit/test_db_repositories.py:117:  url="https://example.test/a"   # DB へ格納する文字列。送信先ではない
$ grep -nE "getenv|environ" src/
(no match)                     # 認証情報どころか環境変数を読むコードが src に無い
```

宣言依存も `click` / `PyYAML` / `pydantic` の3つだけである（`pyproject.toml`）。
`src/` が import する非標準モジュールは `click` / `pydantic` / `yaml` のみ。
`subprocess` の唯一の用途は `doctor` の `git rev-parse` / `git ls-files`（`cli/diagnostics.py:596`。
**ネットワークへ出るサブコマンドを使わない**旨がコードに明記されている）。

**追加の実証（Q6 / C-1 / C-2）**: `socket.socket.__init__` を計装したうえで
`init` / `doctor` / `status` / `run` / `task list` / `agent list` / `doctor --json` を通しで実行した結果、
**作成されたソケットは 0 個**。テストの遮断 fixture に頼らない、製品コード単独での確認である。

`src/media_agent/core/` に `.media-agent` の文字列は出現しない（品質基準 Q7）。

### E-6 完了条件6: 不具合の往復

**org-implementation への不具合レポートは0件、往復は0回。** Blocker / Major を検出しなかったため。
検出した Minor 2件は、いずれも「設計が定めていない挙動」または「T-008 の変更範囲外」であり、
不具合レポートではなくオーケストレーターへの報告として扱った（理由は決定ログ）。

### E-7 GitHub Actions の観測（N-3 / 持ち越し X-2）

**観測できなかった。** 推測で埋めない。

```
$ gh run list
/bin/bash: line 1: gh: command not found
$ curl -sS https://api.github.com/repos/umiji/media-agent/actions/runs
{"message":"GitHub access is not enabled for this session.
 An org admin must connect the Claude GitHub App for this organization."}   # HTTP 403
```

この環境から GitHub API へ到達できないため、`.github/workflows/ci.yml` の実行結果
（成否・所要時間・matrix 各ジョブの結果）は**一切観測していない**。**X-2 は解消していない。**

代替として、CI の手順を**ローカルで matrix 相当に再現した**。

| Python | ruff check | ruff format --check | mypy src | pytest |
| --- | --- | --- | --- | --- |
| 3.11.15 | 0 | 0 | 0 | **538 passed** / 0 |
| 3.12.3 | 0 | 0 | 0 | **538 passed** / 0 |
| 3.13.12 | 0 | 0 | 0 | **538 passed** / 0 |

いずれも repo とは別の fresh venv を作り、`python -m pip install -e ".[dev]"` から始めて
workflow の step と同じ順・同じコマンドで実行した。**「>= 3.11」という宣言（方式設計 3.1）は
3.11 / 3.12 / 3.13 の実行によって裏付けられた。** 未検証のまま残るのは
GitHub Actions の実行環境そのもの（`actions/checkout` / `setup-python` / pip キャッシュ / ubuntu-latest）である。

workflow ファイル自体は YAML として解析でき、job `test` / matrix `["3.11","3.12","3.13"]` /
7 step（Checkout・Set up Python・Install・Lint・Format check・Type check・Test）を持つ。
**Secrets の参照は0件**（PO 制約 C-1 / C-2）。

### E-8 検出した事象（Minor 2件・Blocker / Major は0件）

#### F-1 構造が壊れたプロジェクトで `status` / `run` が終了コード 70 になる（Minor・設計の空白）

再現手順（いずれも `init` 済みのプロジェクトに対して）:

| # | 壊し方 | `doctor` | `status` |
| --- | --- | --- | --- |
| a | `data/media-agent.db` を SQLite でないファイルに置換 | **5**（`[fail] db.file  DB を SQLite として開けません: file is not a database`） | **70** `内部エラーが発生しました: file is not a database` |
| b | `logs/` を通常ファイルに置換 | **5**（`structure.dirs` と `logs.writable` が `[fail]`） | **70** `内部エラーが発生しました: [Errno 17] File exists` |
| c | `data/` を通常ファイルに置換 | **5** | **70** 同上 |

期待する動作の根拠: **明示的な根拠は無い。** 詳細設計 17.2 の「コマンド × 状況の一覧」は
未初期化 / 正常 / `config.yaml` が壊れている / `-C` の指定先が無い の4状況しか定めておらず、
**「プロジェクトの構造や DB が壊れている」状況を定めていない。**

実際の動作: 方式設計 6.5 の「70 = 想定外の内部エラー」に落ちる。Hint は `--verbose` を勧める。

切り分け: `sqlite3.DatabaseError` / `OSError` が `media_agent.errors` の階層へ包まれず、
`cli/app.py` の総括処理まで素通りする（`core/db/connection.py:52` の `sqlite3.connect` /
`:51` の `mkdir`）。**`errors.py` には `DatabaseError`（終了コード 1）が既に存在する**が、
低水準の例外はそこへ写像されていない。同じ DB 由来でも、スキーマ版数の不一致は
`DatabaseVersionError` として正しく包まれ、終了コード 1 と対処 Hint を返す（実測済み）。
つまり**同一サブシステム内で扱いが非対称**である。

判定: 完了条件にも設計にも違反していない。実害は「利用者が `doctor` の存在に誘導されない」ことに留まり、
データ破損は起きない（壊れた DB へ書き込みを試みない）。**Minor。実装の不具合ではなく設計の空白として
オーケストレーターへ上げる**（本ロールの「実装は設計どおりだが期待動作にならない」＝設計側の問題）。

#### F-2 受け入テスト側のヘルパが sqlite 接続を閉じない（Minor・テスト基盤）

再現手順: Python **3.13** で `pytest -q` を実行すると `538 passed, 22 warnings`。
警告はすべて `ResourceWarning: unclosed database`。3.11 / 3.12 では出ない
（unclosed sqlite3 connection の `ResourceWarning` は 3.13 で追加されたため）。

切り分け: `PYTHONTRACEMALLOC` で確保箇所を取ると、**13/13 件が
`tests/acceptance/expectations.py:357`（`build_policy_engine`）→ `core/db/connection.py:52`** である。
`build_runtime_stack` は `runtime_stack` fixture が `conn.close()` するのに対し、
`build_policy_engine` は接続を返さず捨てるため閉じられない。

**製品コードの欠陥ではない。** 製品コードだけで同じ検証をした結果（`ResourceWarning` を
`error` へ昇格させたうえで `init` / `doctor` / `status` / `run`×2 / `task list` を実行）、
**警告は1件も出ない**。CLI は接続を確実に閉じている。

判定: テストは通り、終了コードは 0。実害は 3.13 の CI ログが 22 行汚れることのみ。**Minor。**
`tests/acceptance/expectations.py` の修正は T-008 の変更範囲外のため**直していない。**

### E-9 検証できなかった項目

| # | 項目 | 理由 |
| --- | --- | --- |
| 1 | **GitHub Actions の実行結果**（X-2 / N-3） | この環境から GitHub API へ到達できない（HTTP 403）。`gh` も未インストール。E-7 参照 |
| 2 | 書き込み権限が無いディレクトリでの挙動（`logs/` が読み取り専用など） | **実行ユーザが uid=0 のため `chmod` による権限剥奪が効かない。** 代替として `logs/` を通常ファイルに置換する形で検証した（F-1 の b） |
| 3 | 複数プロセスからの同時 `run` | 詳細設計 D-X4 が「Stage 0 では考慮しない」と明示している。範囲外 |
| 4 | Windows 上の動作 | 持ち越し X-4。今回のゴール外 |
| 5 | PO による実行確認 | ゴール達成条件 A-6 の注記により、組織は PO の確認を待たない |

### E-10 判定

**合格。** 完了条件1〜6 をすべて満たし、Blocker / Major は0件。

| 完了条件 | 判定 | 証拠 |
| --- | --- | --- |
| 1 S-A〜S-I 全通過 | **満たす** | E-1 |
| 2 A-2 の実環境再現 | **満たす** | E-2 |
| 3 全テスト通過 | **満たす** | E-3 |
| 4 S-J / S-K の存在と通過 | **満たす** | E-4 |
| 5 Stage 1 以降の不在確認 | **満たす** | E-5 |
| 6 不具合の往復の記録 | **満たす**（該当0件であることの記録） | E-6 |
