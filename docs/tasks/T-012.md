# T-012: 構造破損時の期待値と設計の空白3件の確定

## メタデータ
- 作成日: 2026-08-24
- 更新日: 2026-08-24
- 状態: 未着手
- 優先度: 高
- 担当: org-design
- 進捗: T-009 のレビュー指摘（RV-1 Major / RV-3 / RV-6）を受けて登録した。未着手
- 依存: 無し（既存の設計成果物への差分であり、他タスクの完了を待たない）
- TDD適用可否: 適用外（設計タスクであり、成果物は文書である）
- 手戻り回数: 0

## 目的

T-009（レビュー）が **Major 1件を含む3件を「設計の空白」と判定した。**
**実装は設計に違反していない。設計が期待値を定めていない。**

このまま org-implementation へ返しても、期待値が無いため往復が収束しない
（`.claude/rules/org-escalation.md`「収束しない往復は、判定基準の不備を示す信号である」）。
**先に設計側で期待値を二値に確定させる。**

## 変更範囲

- `docs/design/stage0-detail.md` への差分（**あなたの持ち物である**）
- 本ファイル `docs/tasks/T-012.md` の 決定ログ / 作業ログ / 証拠 / 進捗

## 確定すべき3件

### D-1（RV-1・Major・最優先）構造が壊れたプロジェクトでの期待値

**詳細設計 17.2「コマンド × 状況の一覧」は4状況しか定めていない。** 次の状況の期待値が無い。

- `.media-agent/data/media-agent.db` が SQLite ファイルでない
- `.media-agent/logs/` が通常ファイルである
- `.media-agent/data/` が通常ファイルである

**現状の実測（T-009 が確認）**: `status` / `run` / `task list` / **`init`** が終了コード **70「内部エラー」**になる。
`sqlite3.DatabaseError` / `OSError` が `media_agent.errors` の階層へ包まれず素通りしている
（`src/media_agent/core/db/connection.py:51-52`、`src/media_agent/project/scaffold.py:129,187`）。

**実害**: `doctor` は同じ状況を終了コード 5 で正確に診断し `media-agent init を実行してください` と助言する。
**その助言に従うと 70 で終わる。利用者から見て復旧経路が行き止まりになっている。**

決めること:

1. 各コマンド（`init` / `doctor` / `status` / `run` / `agent list` / `task list`）が返すべき終了コード
2. 使うべき例外型（`media_agent.errors` の既存の型か、新設か）
3. 利用者へ出すべきメッセージと Hint（**行き止まりにしないこと**）
4. `doctor` が終了コード 5 で診断することとの整合

**同じ DB 由来でも、スキーマ版数の不一致は `DatabaseVersionError` として正しく包まれ終了コード 1 になる。
扱いの非対称を、意図的なものにするのか揃えるのかを決めること。**

### D-2（RV-3）`Agent.version` の永続化の要否

**`Agent.version` がどこにも永続化されない。** `decisions` にも `tasks` にも列が無く、`AuditRecord` にも項目が無い。
用途は `agent list` / `status` の表示だけ（`src/media_agent/cli/rendering.py:99`）。

**要件定義書 20.5 は「同じ Input・設定・Agent Version から実行内容を追跡可能とする」と定めており、
詳細設計 8.1 も `version` を 20.5 の手段と説明している。** 記録側に受け皿が無い。

決めること:

1. Stage 0 で永続化するか、Stage 1 以降へ送るか（**送るなら、要件20.5 を Stage 0 では一部しか満たさないことを明示的に受け入れる判断として記録すること**）
2. 永続化する場合の格納先（`decisions` の列か、`AuditRecord` の項目か、両方か）
3. Migration 版数 2 と `AUDIT_SCHEMA_VERSION` の扱い

**Stage 0 には利用者が存在せず、移行すべき既存データが無い。** 後から足すほど高くつく類の変更である。
**その事実を判断材料に含めること。**

### D-3（RV-6）`task list --limit` の値域

`--status` は値域を検査して `UsageError`（終了コード 2）にするが、`--limit` には下限検査が無い
（`src/media_agent/cli/commands/task.py:33-36`）。`--limit -1` は SQLite の `LIMIT -1`（無制限）になり、
`--limit 0` は行があるのに `(タスクはありません)` と出る。**詳細設計 16.2 に定めが無い。**

決めること: `--limit` の値域と、範囲外の扱い（終了コード）。**`--status` との扱いを揃えるか否か。**

## 完了条件

1. **D-1 / D-2 / D-3 の3件それぞれについて、期待値が二値で判定できる形で `docs/design/stage0-detail.md` に書かれている**
   （どのコマンドが、どの状況で、どの終了コードと、どのメッセージを返すか）
2. **D-1 は 17.2 の一覧へ行として追加されている**（別の場所に書かない。一覧が一覧でなくなる）
3. **各判断に却下案が付いている**（`.claude/rules/org-task-ledger.md`「`却下案` が価値を担う行である」）
4. **D-2 で「Stage 1 以降へ送る」と決めた場合、要件20.5 が Stage 0 で一部しか満たされないことを明記している**
5. 決定が本ファイルの `## 決定ログ` に記録されている
6. `claude/session-resume-mdqrmg` へ push している

## 判断してよい範囲

- **D-1 / D-2 / D-3 の内容そのもの。** 要件定義書24節・28節により、これらは設計フェーズで決めてよい
  （`docs/handover.md`「設計判断を PO へ上げない範囲」）
- 既存の例外階層を拡張するか、既存の型を使い回すか
- 17.2 の表の書き方

## 禁止事項

- **実装しない。** `src/` 配下を変更しない
- **要件定義書（`docs/requirements-media-agent-v0.2.md`）を書き換えない。** PO の持ち物である
- `docs/task-list-media-agent.csv` / `docs/handover.md` / `docs/po-queue.md` / 他タスクの `T-XXX.md` を書き換えない
- **既存の受け入れテスト・単体テストを書き換えない**
- **Stage 1 以降の設計へ広げない。** D-2 で「Stage 1 へ送る」と決めた場合も、送り先の設計まで書かない
- **既存の確定済みの決定を蒸し返さない**（`.claude/rules/org-escalation.md`「上げないもの」）

## テスト方法

設計タスクのため実行によるテストは無い。**完了条件1が「二値で判定できる形か」を自分で点検すること。**
判定できない記述は、T-013（受け入れテストへの変換）で必ず詰まる。

## 停止条件

共通の停止条件に加えて:

- D-1 の期待値が、要件定義書または既存の設計と**矛盾せずには決められない**
- D-2 で、Stage 0 での永続化が **S-2（既存データの破壊・Migration の影響）に該当する**と判明した
  （Stage 0 に既存データは無い想定だが、想定が誤っていた場合）

## 参照すべき成果物

| パス | 何が書いてあるか |
| --- | --- |
| `docs/tasks/T-009.md` の指摘一覧 | RV-1 / RV-3 / RV-6 の詳細と、レビューの判定根拠 |
| `docs/design/stage0-detail.md` 17.2 | 埋めるべき一覧そのもの |
| `docs/design/stage0-architecture.md` 6.5 | 例外階層と、CLI 層を唯一の変換点にする方針 |
| `docs/requirements-media-agent-v0.2.md` 20.5 | D-2 の根拠となる要件 |
| `.claude/rules/quality-standards.md` | 品質基準 Q8（例外階層と終了コード変換） |

## 成果物

（完了時に担当が記入する。ファイルパスとコミット SHA で書くこと）

## ブロッカー

無し。

## 決定ログ

## 作業ログ

## 証拠
