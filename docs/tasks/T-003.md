# T-003: Stage 0 受け入れテスト作成

## メタデータ
- 作成日: 2026-08-23
- 更新日: 2026-08-23
- 状態: テスト作成中
- 優先度: 高
- 担当: org-test
- 進捗: 受け入れテスト S-A〜S-I を `tests/acceptance/` へ作成済み。未実装のため全件が落ちることを確認した（証拠あり）
- 依存: T-002（ブロッカー）— Config Schema・CLI の出力内容・Task の状態遷移が確定しないと、期待値を書けないため
- TDD適用可否: 適用外（このタスク自体が、TDD の「先に書く受け入れテスト」を作る工程である）
- 手戻り回数: 0

## 目的

Stage 0 の完了条件を、**実行可能な受け入れテスト**へ変換する。
この組織は TDD を既定とするため、実装（T-004〜T-007）より先にテストが存在し、**この時点では落ちるのが正しい**。

先に書く理由は、完了条件を後から実装に合わせて緩められないようにするためである。
実装後に書いたテストは、実装の写像になる。

## 変更範囲

- T-001 が定めた Test 構造の配下に、受け入れテストを新規作成する
- テストの実行に必要な最小限の設定ファイル（テスト用の fixture、`conftest.py` 相当）
- 本ファイル `docs/tasks/T-003.md` の 決定ログ / 作業ログ / 証拠 / 進捗

## 禁止事項

- **製品コードを書かない。** テストを通すための実装は T-004〜T-007 の担当である。
  テストが `ImportError` で落ちるからといって、空のモジュールを作って回避しない
- 単体テストを書かない（単体テストは実装エージェントの担当。`docs/glossary.md`「テスト」を参照）
- 設計成果物（`docs/design/` 配下）を書き換えない
- `docs/task-list-media-agent.csv` / `docs/handover.md` / `docs/po-queue.md` / 他タスクの `T-XXX.md` を書き換えない
- **ネットワークへ出るテストを書かない。** Stage 0 に外部接続は存在しない。
  テストが外部へ出るなら、それは設計かテストのどちらかが誤っている
- 認証情報を要求するテストを書かない（PO 制約 C-2）

## 完了条件

以下を全て満たすこと。

1. 受け入れテストが存在し、**T-001 が定めたテスト実行コマンドで収集・実行できる**

   > **重要（T-001 の順序制約 O-2 / `docs/design/stage0-architecture.md` 17.2）**
   > このタスクは T-004 より前に走るため、着手時点で `pyproject.toml` も `media_agent` パッケージも
   > **存在しない**。受け入れテストは収集時に `ModuleNotFoundError` で落ちる。
   > **これが「未実装だから落ちた」証拠として正しい状態である。**
   > 証拠の取り方: `pip install pytest` だけを入れた仮想環境で `pytest tests/acceptance` を実行し、
   > その出力を `## 証拠` へ貼ること。
   > **空のモジュールや `pyproject.toml` を作って収集エラーを回避しないこと**（下記の禁止事項）。
   > 完了条件1 の「収集・実行できる」は、この形で実行できることを指す。
2. 次のシナリオが受け入れテストとして表現されている。これは要件定義書18節 Stage 0 の検証条件そのものである
   - **S-A** 空のディレクトリで `media-agent init` を実行すると `.media-agent/` が生成され、
     設計どおりのファイル（`config.yaml` / `strategy.md` / `rules.md` / `agents/` / `memory/` / `data/` / `logs/`）が揃う
   - **S-B** `init` 済みのプロジェクトで `media-agent doctor` を実行すると、検証項目が全て合格し、
     **終了コード 0** で終わる。**認証情報が無いことを異常として報告しない**
   - **S-C** `init` 済みのプロジェクトで `media-agent status` を実行すると、
     現在の Agent / Task の状態が出力され、終了コード 0 で終わる
   - **S-D** `init` していないディレクトリで `doctor` / `status` を実行すると、
     **明確なエラーメッセージとともに非ゼロの終了コード**で終わる（沈黙して成功しないこと）
3. 次の Core 機能が受け入れテストとして表現されている
   - **S-E** Agent を Runtime へ登録し、Input を渡して実行し、Output を取得できる（設計のダミー Agent を使う）
   - **S-F** Agent 実行が Task として記録され、`pending` → `running` → `completed` の状態遷移が観測できる。
     失敗する Agent を実行すると `failed` になる
   - **S-G** Policy Engine が、設定された `mode`（`auto` / `approval` / `disabled`）と上限値に対して
     設計どおりの判定結果を返す。**少なくとも「許可」「承認が必要」「禁止」「上限超過」の4通り**を確認すること
   - **S-H** Agent の実行が Audit へ記録され、要件定義書5.5節の9項目が追跡できる
   - **S-I** `config.yaml` が壊れている／必須項目が欠けている場合に、**設計どおりのエラーで落ちる**
4. **X API・外部ネットワーク・認証情報を一切使っていない**（実装の禁止事項でもあるが、テスト側でも守ること）
5. **テストを実行し、落ちることを確認して、その実出力を `## 証拠` へ貼っている。**
   「落ちた」という事実と、**落ちた理由が「未実装だから」であること**が読み取れる形で記録すること
6. 各シナリオが、要件定義書のどの節・設計成果物のどの箇所に対応するかが、テスト内のコメントか
   一覧表のどちらかで追える

### T-002 からの申し送り（2026-08-23・オーケストレーター追記）

**期待値の正典は `docs/design/stage0-detail.md` 19章「受け入れテスト S-A〜S-I への対応」である。**
S-A〜S-I の判定の軸はそこに二値で書かれている。推測で期待値を決めないこと。

| # | 申し送り |
| --- | --- |
| **N-1** | **S-A の判定対象に `data/media-agent.db` を含めない。** 方式設計の順序制約 O-1 により DB の生成は `init` に埋め込まれず、T-004 完了時点では DB が存在しない。**DB の存在は S-B（doctor）で判定する** |
| **N-2** | **S-G と S-F の遷移違反は、CLI からは検証できない。** Policy を観測する CLI コマンドは Stage 0 では作らないと決まった（T-007 の範囲を超えるため）。**この2つは公開 API を直接呼ぶ形で書いてよい。** これはオーケストレーターの判断であり、テストのためだけに CLI を増やさない |
| **N-3** | `doctor` の判定は **check id**（`structure.files` 等15個）で行う。**表示文言で判定しないこと。** id は契約、文言は実装の自由（詳細設計 13.2・23章） |
| **N-4** | エラーメッセージの判定は、詳細設計 17.3 の**安定文字列**に限ること。それ以外の文面を `in` で判定すると、文面の改善のたびにテストが落ちる |

## 判断してよい範囲

- テストの分割・ファイル構成・命名（T-001 の Test 構造に従う範囲で）
- fixture の作り方、一時ディレクトリの扱い方
- 上記シナリオを**追加する**こと（Stage 0 の範囲内であれば、気づいた検証を足してよい）
- シナリオの期待値のうち、設計成果物に明記がなく、かつ技術的に一意に定まるもの

**上記シナリオを削ることは判断してよい範囲に含まれない。** 削る必要があると判断したら停止条件へ。

## テスト方法

このタスクの成果物がテストそのものである。検証は完了条件5（落ちることの確認）で行う。

## 停止条件

共通の停止条件に加えて:

- 設計成果物に**期待値が書かれていない**ため、テストの合否を二値で判定できないシナリオがある
  （推測で期待値を決めない。org-design へ問い合わせるか、オーケストレーターへ上げる）
- 完了条件2・3のシナリオのいずれかが、**設計上そもそも実現できない**と判明した
- テストを書くために製品コードを書く必要が出た

## 参照すべき成果物

| パス | 何が書いてあるか |
| --- | --- |
| `docs/requirements-media-agent-v0.2.md` | 要件定義書 v0.2 全文。**18節 Stage 0・19節の対応表・28節を必ず読むこと** |
| `docs/design/stage0-architecture.md` | T-001 の方式設計成果物（構造・技術選定・実行コマンド） |
| `docs/design/stage0-detail.md` | T-002 の詳細設計成果物（Config / DB / Agent Interface / Task / Policy / Logging・Audit） |
| `docs/handover.md` | 今回のゴール達成条件 A-1〜A-8、PO 制約 C-1〜C-4 |
| `CLAUDE.md` | AI開発組織の運営規約 |
| `.claude/rules/org-task-ledger.md` | 決定ログ / 作業ログ / 証拠 の書き方 |
| `.claude/rules/org-escalation.md` | 共通の停止条件とエスカレーション手順 |
| `docs/glossary.md` | この組織で意味が決まっている語 |

**PO 制約（今回のゴール全体に効く）**:

- C-1 X への実際の投稿を行わない
- C-2 認証情報は環境に置かれていない。取得しようとしない
- C-3 投稿処理は、ダミーの送信先か、記録するだけの実装に対して開発する
- C-4 実際の投稿を試す必要が出たら、作業を止めてオーケストレーターへ報告する

**Stage 0 の範囲では投稿処理そのものが存在しない。** 要件定義書28節が「Stage 0 ではまだ X API や
ニュース取得等を実装しない」と明記しているため、ダミー送信先すら作らない。

## 成果物

| パス | 内容 |
| --- | --- |
| `tests/conftest.py` | 全テスト共通。ネットワーク遮断（autouse）・環境分離・`project_dir` fixture（方式設計 10.3 / 10.4） |
| `tests/acceptance/expectations.py` | 判定してよい契約（check id 15個・安定文字列・終了コード表・9キー等）と共通ヘルパ |
| `tests/acceptance/conftest.py` | `cli` / `initialized_project` / `runtime_stack` fixture |
| `tests/acceptance/test_stage0_init.py` | **S-A** |
| `tests/acceptance/test_stage0_doctor.py` | **S-B** |
| `tests/acceptance/test_stage0_status.py` | **S-C** |
| `tests/acceptance/test_stage0_uninitialized.py` | **S-D** |
| `tests/acceptance/test_stage0_agent_runtime.py` | **S-E** |
| `tests/acceptance/test_stage0_task_lifecycle.py` | **S-F** |
| `tests/acceptance/test_stage0_policy.py` | **S-G** |
| `tests/acceptance/test_stage0_audit.py` | **S-H** |
| `tests/acceptance/test_stage0_config_errors.py` | **S-I** |
| `tests/acceptance/test_stage0_entrypoint.py` | 追加（方式設計 10.2 の subprocess 疎通） |
| `tests/acceptance/test_network_guard.py` | 追加（テスト基盤の自己検査。S-A〜S-I ではない） |

### 完了条件6: シナリオと出所の対応（各テストモジュールの docstring にも記載）

| ID | テストモジュール | 期待値の出所 |
| --- | --- | --- |
| S-A | `test_stage0_init.py` | 詳細設計 19章 S-A → 12.1 / 12.3 / 12.5 / 12.6、方式設計7章、要件18節 |
| S-B | `test_stage0_doctor.py` | 詳細設計 19章 S-B → 13.1 / 13.2 / 13.3、PO 制約 C-2 |
| S-C | `test_stage0_status.py` | 詳細設計 19章 S-C → 14章、要件10節 |
| S-D | `test_stage0_uninitialized.py` | 詳細設計 19章 S-D → 17.2 / 17.3、方式設計 6.4 / 6.5 |
| S-E | `test_stage0_agent_runtime.py` | 詳細設計 19章 S-E → 8.1〜8.4 / 15.1〜15.3 / 16.1、要件5.1節 |
| S-F | `test_stage0_task_lifecycle.py` | 詳細設計 19章 S-F → 9.2 / 8.3 / 16.2、要件5.2節・16節 |
| S-G | `test_stage0_policy.py` | 詳細設計 19章 S-G → 10.3 / 10.4 / 10.5 / 10.6、要件5.3節・13節 |
| S-H | `test_stage0_audit.py` | 詳細設計 19章 S-H → 11.1 / 11.3 / 11.4 / 6.4、要件5.5節 |
| S-I | `test_stage0_config_errors.py` | 詳細設計 19章 S-I → 5.4 / 13.4 / 17.2 / 17.3、要件8.2節 |

## ブロッカー

無し（T-002 は 2026-08-23 に `完了`。成果物 `docs/design/stage0-detail.md`）

## 決定ログ

### 2026-08-23 受け入れテストの判定対象を「終了コード・check id・JSON キー・安定文字列」に限定した
- 決定: テキスト出力は**構造**（`[status] <check id>` / `ラベル :` / `動詞  相対パス`）だけを解析し、
  日本語の文面は詳細設計 17.3 の安定文字列だけを `in` で判定する（申し送り N-3・N-4）。
- 却下案: 出力文言の完全一致で判定する → 文面を改善するたびにテストが落ち、実装が文言を直せなくなる
  （用語集「安定文字列」「check id」）。
- 出典: `docs/design/stage0-detail.md` 13.2 / 17.3、`docs/tasks/T-003.md` 申し送り N-3・N-4。

### 2026-08-23 共通ヘルパを `tests/acceptance/expectations.py` に置き、`conftest.py` から import させた
- 決定: 定数・パーサ・公開 API の組み立てをテスト可搬なモジュールへ出し、`tests/__init__.py` と
  `tests/acceptance/__init__.py` を置いて `tests.acceptance.expectations` として import する。
- 却下案: conftest.py に全部置いて `from conftest import ...` する → import 経路が pytest の
  import mode に依存し、`tests/unit/`（T-004 以降が書く）から同じ形で使えない。
- 出典: セッション内の判断、成果物なし。

### 2026-08-23 `media_agent` の import を conftest / expectations のモジュール先頭に置かなかった
- 決定: 製品への import は各テストモジュールの先頭か、fixture・ヘルパの内側で行う。
- 却下案: conftest.py の先頭で import する → T-003 時点では収集エラーが conftest 1件に畳まれ、
  「どのシナリオが未実装で落ちたか」がテスト単位で読めなくなる（順序制約 O-2 の証拠として弱くなる）。
- 出典: `docs/design/stage0-architecture.md` 17.2 の O-2。

### 2026-08-23 ネットワーク遮断はループバックを通し、AF_INET / AF_INET6 の外向きだけを例外にした
- 決定: `socket.socket.connect` / `connect_ex` / `create_connection` / `getaddrinfo` を autouse fixture で
  差し替え、外部ホスト宛だけ `NetworkAccessBlockedError` にする。遮断そのものの自己検査
  （`test_network_guard.py`）も置く。
- 却下案A: ループバックも含めて全遮断 → 将来テスト基盤（カバレッジ収集等）が壊れるだけで、
  PO 制約 C-1・C-2 の担保としては同じ。
- 却下案B: レビューでの目視のみ → 依存ライブラリが内部で通信した場合に検出できない（方式設計 10.4）。
- 出典: `docs/design/stage0-architecture.md` 10.4、PO 制約 C-1・C-2、品質基準 Q6。

### 2026-08-23 S-G と S-F の遷移違反を公開 API で書いた
- 決定: `PolicyEngine.check()` と `TaskService.transition()` を直接呼ぶ。CLI は使わない。
- 却下案: 検証用の CLI（`policy check` 等）を足す → テストのためだけに製品の面を増やすことになる。
  詳細設計 15.4 が明示的に却下している。
- 出典: `docs/tasks/T-003.md` 申し送り N-2（オーケストレーターの判断）、詳細設計 15.4 / 20.3。

## 作業ログ

- 2026-08-23 指示（`docs/tasks/T-003.md`）・要件定義書18節・方式設計（6章 / 7章 / 10章 / 17章）・
  詳細設計（5・8〜17・19章）・用語集4節・品質基準を読み、期待値の所在を19章の表で確認した。
- 2026-08-23 `tests/conftest.py`（遮断・環境分離）と `tests/acceptance/`（契約・fixture）を作成。
- 2026-08-23 S-A〜S-I の受け入れテストを作成。追加で entry point 疎通（方式設計 10.2）と
  遮断 fixture の自己検査を置いた。
- 2026-08-23 `pip install pytest` だけの仮想環境で実行し、全件が未実装で落ちることを確認（証拠）。
- 2026-08-23 `ruff check` / `ruff format --check`（既定設定）が通ることを確認した。

## 証拠

### 実行環境（製品は未インストール。`pytest` だけを入れた仮想環境）

```
$ python3 -m venv <venv> && <venv>/bin/pip install pytest
$ <venv>/bin/pytest --version
pytest 9.1.1
$ git status --porcelain          # 実行前
?? tests/
```

**`pyproject.toml` も `media_agent` パッケージも作っていない**（T-003 の禁止事項、順序制約 O-2）。

### 受け入れテストの実行（未実装のため落ちる＝正しい状態）

```
$ <venv>/bin/pytest tests/acceptance
...
ERROR collecting tests/acceptance/test_stage0_agent_runtime.py
tests/acceptance/test_stage0_agent_runtime.py:16: in <module>
    from media_agent.agents.builtin import EchoAgent, build_default_registry
E   ModuleNotFoundError: No module named 'media_agent'
（test_stage0_audit.py / test_stage0_policy.py / test_stage0_task_lifecycle.py も同じ）
!!!!!!!!!!!!!!!!!!! Interrupted: 4 errors during collection !!!!!!!!!!!!!!!!!!!!
```

収集エラーで打ち切られるため、残りも走らせて全件の落ち方を記録した。

```
$ <venv>/bin/pytest tests/acceptance --continue-on-collection-errors -q
_ ERROR at setup of test_status_fails_with_config_exit_code[yaml-syntax] _
>       from media_agent.cli.app import main
E       ModuleNotFoundError: No module named 'media_agent'
tests/acceptance/conftest.py:39: ModuleNotFoundError
...
2 failed, 3 passed, 7 warnings, 71 errors in 0.54s
```

内訳（`FAILED` / `ERROR` をモジュール別に集計）:

```
  1 ERROR tests/acceptance/test_stage0_agent_runtime.py    # 収集時 ModuleNotFoundError（11 テスト）
  1 ERROR tests/acceptance/test_stage0_audit.py            # 収集時 ModuleNotFoundError（7 テスト）
  1 ERROR tests/acceptance/test_stage0_policy.py           # 収集時 ModuleNotFoundError（14 テスト）
  1 ERROR tests/acceptance/test_stage0_task_lifecycle.py   # 収集時 ModuleNotFoundError（8 テスト）
 26 ERROR tests/acceptance/test_stage0_config_errors.py    # fixture で ModuleNotFoundError
  7 ERROR tests/acceptance/test_stage0_doctor.py           # 同上
  8 ERROR tests/acceptance/test_stage0_init.py             # 同上
  6 ERROR tests/acceptance/test_stage0_status.py           # 同上
 20 ERROR tests/acceptance/test_stage0_uninitialized.py    # 同上
  2 FAILED tests/acceptance/test_stage0_entrypoint.py      # 下記
```

`test_stage0_entrypoint.py` の2件の失敗理由（entry point が未生成であること）:

```
E       ModuleNotFoundError: No module named 'media_agent'
E       AssertionError: <venv>/bin/python: No module named media_agent
E       assert 1 == 0
```

**落ちた理由はすべて「`media_agent` パッケージが存在しないこと」であり、期待値の不一致ではない。**

`3 passed` はテスト基盤の自己検査（`test_network_guard.py`。ネットワーク遮断 fixture が
実際に効いていることの確認）であり、**完了条件を検証するテストではない**。製品の実装状況に
依存しないため、この時点で通るのが正しい。

### 静的検査（既定設定。`pyproject.toml` は T-004 が作る）

```
$ <venv>/bin/ruff check tests/
All checks passed!
$ <venv>/bin/ruff format --check tests/
16 files already formatted
```

### 未検証の項目

- **受け入れテストが「実装後に通る」ことは、この時点では検証できない**（T-004〜T-007 の完了後、
  T-008 で判定する）。本タスクで確認したのは「落ちること」と「落ちた理由」だけである。
- `pytest.ini` 相当の marker 登録（`acceptance`）が未了のため、`PytestUnknownMarkWarning` が7件出る。
  登録先は `pyproject.toml`（方式設計 10.1）であり、T-004 の範囲。
