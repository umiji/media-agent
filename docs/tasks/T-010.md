# T-010: READMEとPO確認用実行手順書

## メタデータ
- 作成日: 2026-08-23
- 更新日: 2026-08-24
- 状態: 完了
- 優先度: 中
- 担当: org-documentation
- 進捗: 完了。利用者向けマスタ8本と生成ビュー2本、PO確認用手順書。A-6 を満たした
- 依存: T-007（ブロッカー）— 実装が固まらないと、動かないコマンドを書いてしまうため
- TDD適用可否: 適用外（文書作成であり、事前にテストへ変換できない）
- 手戻り回数: 0

## 目的

Stage 0 の成果物を、**PO と外部の利用者が実際に動かせる**状態にする。
ゴール達成条件 A-6「PO 確認用の実行手順書が成果物にある」を満たすのがこのタスクである。

PO は 2026-08-23 に「PO 確認用の手順書だけ用意して先へ進む」と判断した。
**したがってこの手順書は、PO が後から一人で Stage 0 を検証するための唯一の入口になる。**
手順書に書かれていないことは、PO には伝わらない。

## 変更範囲

- `README.md`（リポジトリルート。無ければ新規作成）
- `docs/` 配下の利用者向け文書（`docs/verification-stage0.md` など。ファイル名は判断してよい）
- 本ファイル `docs/tasks/T-010.md` の 決定ログ / 作業ログ / 証拠 / 進捗

## 禁止事項

- **設計成果物（`docs/design/` 配下）を書き換えない。** 設計成果物は org-design の持ち物である
- 製品コード・テストコードを書き換えない
- `docs/requirements-media-agent-v0.2.md` / `docs/task-list-media-agent.csv` / `docs/handover.md` /
  `docs/po-queue.md` / 他タスクの `docs/tasks/T-XXX.md` を書き換えない
- `CLAUDE.md` / `.claude/` 配下を書き換えない（層2はオーケストレーターの持ち物）
- **動かないコマンドを書かない。** 書いたコマンドは全て自分で実行して確認すること
- **Stage 1 以降の機能を「できる」と書かない。** 投稿・情報収集・AI 生成は Stage 0 では動作しない

## 完了条件

1. `README.md` に次がある
   - Media Agent が何であるか（要件定義書1.2節の要約）
   - **現在の実装状況が Stage 0 であること**と、Stage 0 で動くもの／動かないものの区別
   - インストール手順
   - 基本的な使い方（`init` / `doctor` / `status` / `run`）
   - 開発者向け: テストの実行方法、リントの実行方法
2. **PO 確認用の実行手順書がある。** 次を満たすこと
   - 前提（必要な Python バージョン、リポジトリの取得方法）から書かれている
   - **コピーして順に実行できるコマンド列**になっている
   - 各コマンドの**期待される出力と終了コード**が書かれている
   - 要件定義書18節 Stage 0 の検証条件（`init` → Project生成 → `doctor` → `status`）を必ず含む
   - 期待どおりにならなかった場合、どこへ報告すればよいかが書かれている
3. **手順書に書いた全コマンドを実際に実行し、記載どおりに動くことを確認している。**
   実行結果を `## 証拠` へ記録すること
4. **認証情報が不要であることが明記されている。** Stage 0 は外部サービスへ接続しないため
5. 記述が実装と食い違っている箇所を見つけた場合、**文書側で辻褄を合わせず**、報告に含める

### T-007 からの接続点（2026-08-24・オーケストレーター追記）

T-007 は完了済み（コミット `2f12cbd` / `52b98ee`、記録 `e1f64d1`）。

| # | 内容 |
| --- | --- |
| N-7 | **5コマンド（`doctor` / `status` / `run` / `agent list` / `task list`）の実出力・終了コード・`--json` のスキーマが確定した。** `docs/tasks/T-007.md` の `## 証拠` に、一時ディレクトリで実際に実行した出力がある。**手順書の例はそこから取ること。自分で書き起こして食い違わせない** |
| N-8 | `pyproject.toml` に **`readme` が宣言されていない**（T-004 の判断。README.md が存在しないと hatchling がビルドに失敗するため）。該当行にコメントが残っている。**README.md を作ったら、この宣言を戻すところまでが本タスクである** |
| N-9 | スタブコマンド（終了コード 10）と、Stage 0 で実装済みのコマンドの区別は `docs/glossary.md` にある。**「未実装の枠」（終了コード 70）は T-007 で全て消えた。** 70 を返すコマンドはもう無い |
| N-10 | 公開 API を記載範囲に含めるなら、`media_agent.core.task` / `core.runtime` / `core.policy` / `agents.builtin` の公開物と、組み込み Agent 2種（`echo` / `fail`）が対象になる |

## 判断してよい範囲

- 文書のファイル分割・章立て・言語（日本語で書くこと。それ以外は自由）
- README に含める図・表
- 手順書のファイル名と置き場所（`docs/` 配下であれば）
- Stage 1 以降のロードマップを README に載せるかどうか

## テスト方法

- 実環境確認: **必須**。手順書のコマンドを、記載順に、実際に実行する。実施者は担当自身
- 自動テストは無い

## 停止条件

共通の停止条件に加えて:

- **手順書に書こうとしたコマンドが実際には動かない**（文書を実装に合わせて曲げず、報告する）
- 実装が設計成果物と食い違っており、どちらを説明すべきか判断できない

## 参照すべき成果物

| パス | 何が書いてあるか |
| --- | --- |
| `docs/requirements-media-agent-v0.2.md` | 要件定義書 v0.2 全文。**18節 Stage 0・19節の対応表・28節を必ず読むこと** |
| `docs/design/stage0-architecture.md` | T-001 の方式設計成果物（構造・技術選定・実行コマンド） |
| `docs/design/stage0-detail.md` | T-002 の詳細設計成果物（Config / DB / Agent Interface / Task / Policy / Logging・Audit） |
| `docs/handover.md` | 今回のゴール達成条件 A-1〜A-8、PO 制約 C-1〜C-4 |
| `CLAUDE.md` | AI開発組織の運営規約 |
| `.claude/rules/org-task-ledger.md` | 決定ログ / 作業ログ / 証拠 の書き方 |
| `.claude/rules/org-escalation.md` | 共通の停止条件とエスカレーション手順 |
| `docs/glossary.md` | この組織で意味が決まっている語 |

**PO 制約（今回のゴール全体に効く）**:

- C-1 X への実際の投稿を行わない
- C-2 認証情報は環境に置かれていない。取得しようとしない
- C-3 投稿処理は、ダミーの送信先か、記録するだけの実装に対して開発する
- C-4 実際の投稿を試す必要が出たら、作業を止めてオーケストレーターへ報告する

**Stage 0 の範囲では投稿処理そのものが存在しない。** 要件定義書28節が「Stage 0 ではまだ X API や
ニュース取得等を実装しない」と明記しているため、ダミー送信先すら作らない。

なお、`docs/design/` 配下の実際のファイル名は T-001 / T-002 の成果物に従うこと。上表は想定である。

## 成果物

| パス | 位置づけ | 想定読者 |
| --- | --- | --- |
| `docs/features/overview.md` | マスタ | 利用者・将来の開発者 |
| `docs/features/stage0-scope.md` | マスタ（何が動き、何が動かないか） | 利用者・PO |
| `docs/features/cli.md` | マスタ（CLI リファレンス） | 利用者 |
| `docs/features/project-directory.md` | マスタ（`.media-agent/` と `config.yaml`） | 利用者 |
| `docs/guides/install.md` | マスタ | 利用者 |
| `docs/guides/quickstart.md` | マスタ | 利用者 |
| `docs/guides/verification-stage0.md` | **マスタ。ゴール達成条件 A-6 の本体** | **PO** |
| `docs/guides/development.md` | マスタ | 開発者 |
| `README.md` | **生成物**（人間用ビュー） | リポジトリを開いた人 |
| `docs/handbook.md` | **生成物**（通し読み用） | 通しで読みたい人 |
| `docs/tools/build_docs.py` | 生成スクリプト | 保守者 |
| `pyproject.toml` | `readme = "README.md"` を宣言（N-8） | — |

コミット: 下記「作業ログ」参照。

## ブロッカー

無し（T-007 は `完了`）。

## 決定ログ

### 2026-08-24 利用者向け文書を「マスタ + 生成ビュー」の2階建てにした

- 決定: 正典を `docs/features/` と `docs/guides/` の**機能・目的単位のマスタ**に置き、`README.md` と
  `docs/handbook.md` は `docs/tools/build_docs.py` が**マスタから生成する**構成にした。
  生成物には「直接編集しないこと」の注記を埋め込み、`--check` で最新かどうかを検査できるようにした
- 却下案: README を手で書き、`docs/` にも同じ説明を置く → 同じ内容を2箇所で手書きすることになり、
  実装が変わったときに片方だけ直されて必ず乖離する。乖離した README は
  「実装と食い違う記述」として次のドキュメントタスクの手戻りになる
- 却下案: 文書を README 1枚に集約する → エージェントが特定機能の記述だけを読めなくなり、
  複数のドキュメントタスクが同じファイルを取り合う
- 付帯条件: 生成スクリプトの置き場所をタスクの `変更範囲`（`docs/` 配下）に収めるため
  `docs/tools/build_docs.py` とした。**`.claude/` 配下には置かない**（層2はオーケストレーターの持ち物）。
  ruff / mypy / pytest はいずれも `docs/` を対象外にしているため、品質基準 Q1〜Q3 への影響は無い
- 出典: セッション内の判断、成果物なし（`org-documentation` の役割定義「人間用ビューは生成する」に従う）

### 2026-08-24 インストール手順を「PyPI から」ではなく「ソースから」にした

- 決定: `git clone` → `python3 -m venv` → `pip install -e ".[dev]"` を正典の手順として書いた
- 却下案: 要件定義書3.2節の `pip install media-agent` をそのまま書く → **Package 配布は Stage 8 であり、
  現在 PyPI に存在しない。** 実行できない手順を書くことは、このタスクの禁止事項
  「動かないコマンドを書かない」に反する
- 出典: 要件定義書 19節 Stage 対応表（Package配布 = S8）/ 実地確認（下記 証拠 E-2）

### 2026-08-24 手順書に「失敗する Agent」「未実装コマンド」「未初期化」の確認を含めた

- 決定: `run --agent fail`（終了コード 1）、`post` 等のスタブ（終了コード 10）、
  未初期化ディレクトリ（終了コード 3）を手順に入れた
- 却下案: 正常系（`init` → `doctor` → `status`）だけを書く → 要件定義書18節の検証条件は満たすが、
  **「未実装のコマンドが黙って成功しないこと」を PO が確認できない。**
  Stage 0 でスタブを終了コード 10 にした判断（方式設計 6.4）が、確認されないまま残る
- 出典: 方式設計 6.4 / 6.5、`docs/glossary.md`「スタブコマンド」

## 作業ログ

- 2026-08-24 指示の充足を確認（完了条件・判断してよい範囲・参照すべき成果物がそろっている）。
  着手時の作業ツリーは clean、HEAD は `33a6541`
- 2026-08-24 要件定義書1.2 / 10 / 18 / 19 / 28節、方式設計 4〜7章・6.5、詳細設計、`docs/glossary.md`、
  T-007 / T-008 の `## 証拠` を読んだ
- 2026-08-24 一時ディレクトリで 5 コマンドを自分で実行し、T-007 / T-008 の記録と一致することを確認した
- 2026-08-24 マスタ8本を作成し、`docs/tools/build_docs.py` で `README.md` / `docs/handbook.md` を生成した
- 2026-08-24 `pyproject.toml` の `readme = "README.md"` を戻し、`python -m build` が通ることを確認した
- 2026-08-24 **GitHub から新しく clone した環境で、手順書を上から順に全て実行した**（証拠 E-2）

## 証拠

### E-1 リポジトリ側の品質基準（Q1〜Q3）に影響が無いこと

`pyproject.toml` に `readme` を戻したうえで、リポジトリのルートで実行した。

```
$ PATH="$PWD/.venv/bin:$PATH" .venv/bin/pytest -q
538 passed in 10.77s              （終了コード 0）
$ .venv/bin/ruff check .
All checks passed!                （終了コード 0）
$ .venv/bin/ruff format --check .
97 files already formatted        （終了コード 0）
$ .venv/bin/mypy src
Success: no issues found in 49 source files   （終了コード 0）
```

### E-2 手順書の全コマンドを、新しい clone で上から順に実行した

`git clone https://github.com/umiji/media-agent.git` で取得した作業ツリー（`33a6541`）に対し、
`docs/guides/verification-stage0.md` の手順 1〜13 をそのまま実行した。**記載どおりに動いた。**

| 手順 | コマンド | 実際の結果 |
| --- | --- | --- |
| 1 | `git clone` → `git checkout claude/session-resume-mdqrmg` | 成功。`33a6541` |
| 2 | `python3 -m venv .venv` → `pip install -e ".[dev]"` | `Successfully installed ... media-agent-0.1.0 ...` |
| 3 | `media-agent --version` | `media-agent, version 0.1.0` / 終了コード 0 |
| 3 | `media-agent --help` | 未実装4コマンドが「Stage 0 では未実装」と表示される |
| 5 | `media-agent init` | 9 行の `created` / 終了コード 0。`find` の結果は記載の7ファイルと一致 |
| 5 | `cat .media-agent/config.yaml` | `project.name` がディレクトリ名（`demo-project`）。認証情報の欄は無い |
| 6 | `media-agent doctor` | `結果: 14 ok / 0 warn / 1 skipped / 0 fail` / 終了コード 0 |
| 7 | `media-agent status` | `tasks : total 0 ...` / `(タスクはありません)` / 終了コード 0 |
| 8 | `media-agent agent list` | `echo` / `fail` の2件 |
| 8 | `media-agent run` → `status` → `task list` → `cat audit.jsonl` | `completed` / `total 1` / 1行 / 監査記録1行。すべて終了コード 0 |
| 9 | `media-agent run --agent fail` | `status : failed` / **終了コード 1**。`task list --status failed` に残る |
| 10 | `media-agent post` | 未実装メッセージ / **終了コード 10**。`setup` / `research` / `analyze` も 10 |
| 11 | 未初期化ディレクトリで `media-agent status` | `Media Agent が初期化されていません` / **終了コード 3** |
| 12 | `pip list \| grep -Ei 'requests\|httpx\|...'` | `MATCH=1`（一致なし） |
| 12 | `grep -rn 'getenv\|environ' src/` | `MATCH=1`（一致なし） |
| 13 | `pytest -q` / `ruff` / `mypy` | `538 passed` / `All checks passed!` / `96 files already formatted` / `Success:` すべて終了コード 0 |

5 コマンドの実出力は T-007 の `## 証拠` および T-008 の `## 証拠` E-2 と一致した。
**手順書の出力例はそれらから取っており、書き起こしによる食い違いは無い。**

### E-3 完了条件2: 手順書が満たしている項目

| 完了条件2の要求 | 該当箇所（`docs/guides/verification-stage0.md`） |
| --- | --- |
| 前提から書かれている | 「前提」（Python 3.11 以上 / Git）・手順1（`git clone`） |
| コピーして順に実行できる | 手順1〜14 がすべて `sh` のコードブロック |
| 期待される出力と終了コード | 各手順に「期待される出力」と `echo "EXIT=$?"` |
| 要件18節の検証条件を含む | 手順5（`init` → Project 生成）・手順6（`doctor`）・手順7（`status`） |
| 期待どおりにならなかった場合の報告先 | 「期待どおりにならなかった場合」（控える情報・追加情報・渡す先） |

### E-4 完了条件4: 認証情報が不要であることの明記

| 文書 | 記述 |
| --- | --- |
| `docs/guides/verification-stage0.md` | 冒頭「認証情報は不要である」（**入力を求められたら異常**と明記）、手順6の `[skipped]`、手順12 |
| `docs/features/stage0-scope.md` | 「認証情報は要らない」節 |
| `docs/guides/install.md` | 前提の表に「認証情報 — 不要」 |
| `README.md`（生成物） | 冒頭の注意書き |

### E-5 完了条件（N-8）: `readme` 宣言を戻し、ビルドが通ること

```
$ python -m build --outdir build-check /home/user/media-agent
Successfully built media_agent-0.1.0.tar.gz and media_agent-0.1.0-py3-none-any.whl
（終了コード 0）
```

wheel の `METADATA` に `Description-Content-Type: text/markdown` と README 本文が入っていることを確認した。

### E-6 生成物がマスタと一致していること

```
$ python3 docs/tools/build_docs.py --check
生成物はマスタと一致しています      （終了コード 0）
```
