# T-004: Package骨格・CLI・Config・init実装

## メタデータ
- 作成日: 2026-08-23
- 更新日: 2026-08-24
- 状態: 実装中
- 優先度: 高
- 担当: org-implementation
- 進捗: 実装完了。S-A / S-D が全通過、S-I は T-007 依存の7件を除き通過。ruff / mypy / 単体テストは通過
- 依存: T-003（ブロッカー）— TDD 適用タスクのため、先に落ちる受け入れテストが存在している必要がある
- TDD適用可否: 適用（T-003 の受け入れテスト S-A / S-D / S-I を通すことが目標）
- 手戻り回数: 0

## 目的

Media Agent を「Python パッケージとして import でき、CLI として起動でき、任意のプロジェクトへ導入できる」
状態にする。要件定義書 Stage 0 の土台であり、T-005 以降の全実装がこの上に乗る。

要件定義書28節が Stage 0 のゴールとして挙げる
«「Media Agentという汎用基盤を、任意のプロジェクトに導入して動かせる」» の、**導入部分**がここである。

## 変更範囲

- Python パッケージ本体のうち、次の領域（具体的なパスは T-001 の方式設計に従う）
  - パッケージ骨格（`pyproject.toml` 等のパッケージ定義、バージョン管理）
  - **ruff / mypy / pytest の設定**（T-001 の差分 D-1。`pyproject.toml` 内に置く。
    全実装タスクの共通完了条件が「T-001 が定めたリントコマンドが通る」である以上、
    設定は T-004 の時点で存在していなければならない。T-007 まで待てない）
  - CLI エントリポイントとコマンド登録の仕組み
  - Config の読み込み・スキーマ検証
  - `media-agent init`（`.media-agent/` の生成）
- `.gitignore`（要件定義書14.2節。Credential をコミットしないための設定）
- 上記に対応する単体テスト
- 本ファイル `docs/tasks/T-004.md` の 決定ログ / 作業ログ / 証拠 / 進捗

**DB・Logging/Audit（T-005）、Agent Runtime・Task・Policy（T-006）、doctor/status/run の中身（T-007）は
このタスクの範囲外。** CLI にコマンドを登録する枠だけ用意し、中身は次タスクへ渡してよい。

## 完了条件

1. パッケージが T-001 の定めた方式でインストールでき、**`media-agent --help` が動作する**
2. **要件定義書10節の10コマンドが CLI に登録されている。**
   T-001 が定めた切り分けに従い、Stage 0 で実装しないコマンド（`post` / `research` / `analyze` など）は
   「未実装である」と明示するメッセージを出して終了すること。**沈黙して成功しないこと**
3. `media-agent init` が、要件定義書8.1節の `.media-agent/` 構造を生成する
   （`config.yaml` / `strategy.md` / `rules.md` / `agents/` / `memory/` / `data/` / `logs/`）。
   生成物の中身は T-002 の詳細設計に従う
4. 既に `.media-agent/` が存在する場合の挙動が、T-002 の設計どおりに実装されている。
   **`init --force` を含む**（T-002 の範囲追記。詳細設計 12.5。完了条件4 の対象内であり、境界の変更ではない）
5. Config の読み込みとスキーマ検証が実装され、**壊れた `config.yaml` / 必須項目の欠落に対して
   設計どおりのエラーで落ちる**（受け入れテスト S-I）
6. `init` していないディレクトリでコマンドを実行したとき、**明確なエラーメッセージと非ゼロ終了コード**になる
   （受け入れテスト S-D。ただし `init` 自身と `--help` を除く）
7. **受け入れテスト S-A / S-D / S-I が通る**
8. `.gitignore` により、Credential ファイル（`.env` 等）がコミット対象から除外されている

### 全実装タスク共通の完了条件

上記に加えて、次を全て満たすこと。

- **自分が書いたコードに単体テストがある**（受け入れテストは T-003 の担当。単体テストは実装の担当）
- T-001 が定めたテスト実行コマンドとリントコマンドが**通る**
- **担当範囲の受け入れテストが通る。** 他タスクの範囲に依存して落ちているものは、
  どのテストがなぜ落ちているかを `## 証拠` に明記して残す（そのまま次タスクへ引き継がれる）
- 論理的なまとまりごとに commit し、**タスク完了時に `claude/mvp01-implementation-testing-0ldwtq` へ push している**
  （レビュー通過を待たない。`CLAUDE.md` の Git 規約）
- `## 証拠` に、テスト実行コマンドと**実出力**、コミット SHA が記録されている

### T-003 からの申し送り（2026-08-23・オーケストレーター追記）

受け入れテストは **`tests/acceptance/` に既に存在し、全件落ちている**（コミット `7277a27`）。
**テストを書き換えて通してはならない**（禁止事項）。したがって、T-003 が設計の記述だけでは決まらず
**自分で決めた次の期待値は、実装側が合わせるべき契約**である。設計と矛盾すると判断した場合は、
直さずに停止条件として報告すること。

| # | 契約 | 根拠（T-003 の判断） |
| --- | --- | --- |
| K-1 | `media_agent.core.task` / `core.runtime` / `core.policy` / `agents.builtin` の主な公開物を、**`__init__.py` で再エクスポートする**（`from media_agent.core.task import TaskService` の形で import できること） | 詳細設計4章が「モジュール＝ディレクトリ、主な公開物＝その型」と対応付けている |
| K-2 | Repository のコンストラクタは `TaskRepository(conn)` / `DecisionRepository(conn)`（**位置引数**で `sqlite3.Connection`） | 詳細設計 6.6「すべて `sqlite3.Connection` を受け取り」。`__init__` の明示が無い |
| K-3 | `run --json` は**失敗時（終了コード1）にも** stdout へ JSON を出す | 15.3 の JSON に `error` キーがある以上、失敗時に出ないと `error` が無意味 |
| K-4 | 未初期化時の `doctor` / `status` / `run` / `agent list` / `task list` は、**stdout が空**（エラーは stderr） | 19章「stdout に成功を示す出力を出さない」 |
| K-5 | `run` の結果は**別プロセス（別接続）からも見える**。すなわちコミットが要る | 14章の出力例が実行済み Task の集計を示している |
| K-6 | `doctor --json` の `project_root` は `resolve()` 後のパスと比較できること | 絶対パスとしか書かれていないため、symlink 差で落ちないようにした |

**T-004 で対応が要るもの（T-003 が実際に踏んだ差分）**

| # | 内容 |
| --- | --- |
| M-1 | **`pyproject.toml` に `acceptance` marker を登録する**（方式設計 10.1）。未登録のため現在 `PytestUnknownMarkWarning` が出ている |
| M-2 | **`tests/` は package になっている**（`tests/__init__.py` / `tests/acceptance/__init__.py`）。`tests.acceptance.expectations` を import するため。`tests/unit/` も同じ形にすること。mypy の対象は `src` のみなので影響は無い |
| M-3 | **ruff に `known-first-party = ["media_agent"]` を設定する**と、既存テストの import 順に整形差分が出る可能性がある。**整形のみの差分は許容する**（テストの意味を変えないこと） |
| M-4 | **罠**: 詳細設計 12.3「生成した `config.yaml` を `init` の最後に検証する」の**検証対象は、`init` 自身が生成したものに限る**。既存の壊れた `config.yaml` まで検証すると、17.2 の「壊れた設定でも `init` は終了コード 0」と食い違う。T-003 はこの読みでテストを書いている |

## 判断してよい範囲

- 関数・クラス・モジュールの分割と命名（T-001 のパッケージ構造に従う範囲で）
- エラーメッセージの文面（設計に文面の指定が無い場合）
- 単体テストの粒度と構成
- 設計成果物に書かれていない実装詳細のうち、技術的に一意に定まるもの

**設計成果物に書かれている事項を変更することは、判断してよい範囲に含まれない。**

## 禁止事項

- **受け入れテスト（T-003 が書いたテスト）を書き換えて通さない。** テストが誤っていると判断したら、
  直さずに停止条件として報告する。`.claude/rules/org-task-ledger.md`「やってはいけないこと」に該当する
- 設計成果物（`docs/design/` 配下）を書き換えない。読み方に迷ったら org-design へ問い合わせる（1往復まで）
- `docs/requirements-media-agent-v0.2.md` / `docs/task-list-media-agent.csv` / `docs/handover.md` /
  `docs/po-queue.md` / 他タスクの `docs/tasks/T-XXX.md` を書き換えない
- **X API クライアントを書かない。外部ネットワークへ送信するコードを書かない**（PO 制約 C-1・C-3、
  および要件定義書28節「Stage 0ではまだX APIやニュース取得等を実装しない」）
- **認証情報を必要とする実装をしない。** 認証情報をリポジトリへ置かない（PO 制約 C-2、要件定義書14.2節）
- **Stage 1 以降の機能を実装しない。** Content / Research / Strategy / Analytics / Engagement / Orchestrator の
  各 Agent、Google News / RSS / Web Connector、過去投稿 Import、重複チェック、AI 呼び出しは範囲外
- **本タスクの `変更範囲` の外にあるファイルを変更しない。** 他の実装タスクが担当する領域に不足を見つけても、
  その場で直さずに報告する（`CLAUDE.md`「スコープを勝手に広げない」）
- スコープ外のリファクタリング・警告修正をしない

## テスト方法

- 単体テスト: Config の読み込み・検証、`init` のファイル生成、CLI の引数解釈
- 受け入れテスト: T-003 が書いた S-A / S-D / S-I を実行する
- 実環境確認: **要**。実際に一時ディレクトリで `media-agent init` を実行し、生成物を確認する。実施者は担当自身
- 回帰: このタスクでは対象なし（最初の実装のため）

## 停止条件

共通の停止条件に加えて:

- 受け入れテストの期待値と設計成果物が**食い違っている**（テストを書き換えて解決しない）
- 設計成果物に、実装を一意に決められない箇所がある（org-design へ1往復まで問い合わせ、解決しなければ報告）
- Stage 0 の範囲で、外部ネットワークまたは認証情報が必要になった（PO 制約 C-1・C-2 に抵触する）

## 参照すべき成果物

| パス | 何が書いてあるか |
| --- | --- |
| `docs/requirements-media-agent-v0.2.md` | 要件定義書 v0.2 全文。**18節 Stage 0・19節の対応表・28節を必ず読むこと** |
| `docs/design/stage0-architecture.md` | T-001 の方式設計成果物（構造・技術選定・実行コマンド） |
| `docs/design/stage0-detail.md` | T-002 の詳細設計成果物（Config / DB / Agent Interface / Task / Policy / Logging・Audit） |
| `docs/handover.md` | 今回のゴール達成条件 A-1〜A-8、PO 制約 C-1〜C-4 |
| `CLAUDE.md` | AI開発組織の運営規約 |
| `.claude/rules/org-task-ledger.md` | 決定ログ / 作業ログ / 証拠 の書き方 |
| `.claude/rules/org-escalation.md` | 共通の停止条件とエスカレーション手順 |
| `docs/glossary.md` | この組織で意味が決まっている語 |

**PO 制約（今回のゴール全体に効く）**:

- C-1 X への実際の投稿を行わない
- C-2 認証情報は環境に置かれていない。取得しようとしない
- C-3 投稿処理は、ダミーの送信先か、記録するだけの実装に対して開発する
- C-4 実際の投稿を試す必要が出たら、作業を止めてオーケストレーターへ報告する

**Stage 0 の範囲では投稿処理そのものが存在しない。** 要件定義書28節が「Stage 0 ではまだ X API や
ニュース取得等を実装しない」と明記しているため、ダミー送信先すら作らない。

なお、`docs/design/` 配下の実際のファイル名は T-001 / T-002 の成果物に従うこと。上表は想定である。

## 成果物

| 種別 | パス | コミット |
| --- | --- | --- |
| パッケージ定義・ツール設定 | `pyproject.toml` | `74c502e` |
| リポジトリの除外設定 | `.gitignore` | `74c502e` |
| 版数の単一の真実 / `python -m` 入口 | `src/media_agent/__init__.py` / `__main__.py` | `74c502e` |
| 例外階層（詳細設計 17.1） | `src/media_agent/errors.py` | `fdca869` |
| Config Schema と読み込み（詳細設計5章） | `src/media_agent/core/config/{models,loader}.py` | `fdca869` |
| ProjectLayout（詳細設計 4.1） | `src/media_agent/project/layout.py` | `243648e` |
| `init` のスキャフォールド（詳細設計12章） | `src/media_agent/project/scaffold.py` | `243648e` |
| 生成テンプレート（詳細設計 12.2） | `src/media_agent/project/templates/`（4ファイル） | `243648e` |
| CLI と10コマンドの登録（方式設計6章） | `src/media_agent/cli/`（app / context / commands 8ファイル） | `fcc961e` |
| 単体テスト（115ケース） | `tests/unit/`（6ファイル） | `6abba15` |
| 受け入れテストの整形（申し送り M-3） | `tests/acceptance/` の4ファイル（import 順のみ） | `3bab645` |

## ブロッカー

無し（T-003 は 2026-08-23 に `完了`。受け入れテストは `tests/acceptance/` にあり、全件落ちている）

## 決定ログ

### 2026-08-24 T-007 担当コマンドの「枠」を、プロジェクト解決と Config 読み込みまで実装する

- 決定: `doctor` / `status` / `run` / `agent list` / `task list` は、詳細設計 17.2 が定める
  **失敗系（未初期化=3、設定不正=4）までを T-004 で実装**し、本体は `NotImplementedError`
  （CLI が終了コード 70 へ変換）で止める。`doctor` は Config を読まない（罠 D-T15）、
  `agent list` も Config を読まない（17.2 の注記）
- 却下案A: 枠を「終了コード 10 のスタブ」にする → **スタブコマンドの定義（Stage 0 では実装しない）と
  食い違う。** これらは Stage 0 の実装対象であり、10 を返すと用語集の「スタブコマンド」が二義的になる
- 却下案B: 枠を作らずコマンド自体を登録しない → 完了条件2（10コマンドの登録）と
  受け入れテスト S-D（未初期化で終了コード 3）を満たせない
- 却下案C: 中身まで実装する → T-005〜T-007 の変更範囲を侵す（禁止事項）
- 付帯条件: 本体の到達点は `cli/commands/_pending.py` の1関数に集約し、T-007 が置き換える場所を
  1か所に絞った。**終了コード 0 で黙って成功する経路は作っていない**
- 出典: セッション内の判断。コミット `fcc961e`

### 2026-08-24 ruff の `select` から E501（行長）を外し、`.claude/` と `docs/` を検査対象外にする

- 決定: `select = ["E4", "E7", "E9", "F", "W", "I", "UP", "B", "C4"]`（ruff の既定に W / I / UP / B / C4 を追加）。
  `extend-exclude = [".claude", "docs"]`
- 却下案A: `E` 全体（E501 を含む）を選ぶ → **T-003 が書いた受け入れテストの docstring が 101 件違反する。**
  `ruff format` は文字列を折らないため自動解消できず、テストの文面を書き換えるしかない（禁止事項）。
  行長は整形器の担当とし、検査からは外す（ruff の既定と同じ方針）
- 却下案B: `.claude/scripts/org-check.py` を整形して通す → 組織の運営ファイルであり、
  本タスクの変更範囲外（方式設計4章「`.claude/` は製品の一部ではない」）
- 却下案C: `docs/` を検査対象に残す → **`ruff format` が Markdown 中の Python コードブロックを
  書き換える**ことを実際に確認した（`docs/design/stage0-detail.md` 4.1 が整形された）。設計成果物の
  改変は禁止事項であるため、除外する。実行後に `git checkout` で復旧済み
- 出典: セッション内の判断。コミット `74c502e`

### 2026-08-24 `pyproject.toml` に `readme` を宣言しない

- 決定: `readme = "README.md"` を書かない（コメントで理由を残した）
- 却下案: README.md を作って宣言する → **README は org-documentation（T-010）の成果物**であり、
  実装が中身を先に決めることになる。hatchling は宣言したファイルが無いとビルドが落ちるため、
  「宣言しない」が唯一整合する選択
- 出典: セッション内の判断。コミット `74c502e`

### 2026-08-24 プロジェクトルート解決に `resolve_project_root()` を足す

- 決定: 詳細設計 4.1 の `find_project_root(start)`（上方探索）に加えて、
  `resolve_project_root(explicit, *, cwd)` を `project/layout.py` へ置く
- 却下案A: `find_project_root` にキーワード引数を足す → 設計が示したシグネチャを変えることになる
- 却下案B: CLI 側で `-C` の判定を書く → `.media-agent` の有無を CLI が知ることになり、品質基準 Q7 の
  「パス解決を Project 層に閉じ込める」という機構が崩れる
- 出典: セッション内の判断。コミット `243648e`

### 2026-08-24 `--json` をグローバルとコマンドの両方に置く

- 決定: グループの `--json`（方式設計 6.2）に加えて、`doctor` / `status` / `run` / `agent list` /
  `task list` にも `--json` を登録する。`CliContext.wants_json()` が両者を合成する
- 却下案: グローバルのみ → 受け入れテストが `agent list --json` の形で呼んでいる（詳細設計 15.3 が
  対応コマンドを広げたため）。サブコマンド側に無いと終了コード 2 になる
- 出典: 詳細設計 15.3 / `tests/acceptance/test_stage0_config_errors.py:213`。コミット `fcc961e`

## 作業ログ

- 2026-08-24 着手前に `git status` を確認（差分なし）、受け入れテストの落ち方を記録（54 errors、
  すべて `ModuleNotFoundError: No module named 'media_agent'`）
- `pyproject.toml` → `errors.py` → `core/config/` → `project/`（layout / scaffold / templates）→
  `cli/`（app / context / commands）の順に実装した
- 各モジュールに単体テストを書いた（`tests/unit/`、115ケース）
- 実環境確認: `.venv/bin/media-agent init` を `/tmp/ma-demo` で実行し、生成物・冪等性・
  未初期化・スタブ・壊れた設定の5点を目視で確認した（証拠に転記）
- `ruff check .` / `ruff format --check .` / `mypy src` を通した

## 証拠

### 着手時（実装前）の受け入れテスト

```
$ .venv/bin/python -m pytest tests/acceptance/test_stage0_init.py \
    tests/acceptance/test_stage0_uninitialized.py tests/acceptance/test_stage0_config_errors.py -q
54 errors in 0.28s
E       ModuleNotFoundError: No module named 'media_agent'
```

### 完了条件1: `media-agent --help` が動作する

```
$ .venv/bin/python -m pip install -e ".[dev]"
Successfully installed ... media-agent-0.1.0 ...
$ .venv/bin/media-agent --version
media-agent, version 0.1.0
$ .venv/bin/media-agent --help   # 終了コード 0
Commands:
  agent     登録された Agent を確認する。
  analyze   （Stage 5 で実装予定。Stage 0 では未実装）
  doctor    設定・構造・依存関係を検証する。
  init      現在のプロジェクトに Media Agent を導入する。
  post      （Stage 3 で実装予定。Stage 0 では未実装）
  research  （Stage 2 で実装予定。Stage 0 では未実装）
  run       Media Agent の Workflow を実行する。
  setup     （Stage 3 で実装予定。Stage 0 では未実装）
  status    現在の Agent / Task の状態を確認する。
  task      記録された Task を確認する。
```

`python -m media_agent --help` も同じ出力になる（`tests/acceptance/test_stage0_entrypoint.py` で確認）。

### 完了条件3・4: 実環境での `init`（`/tmp/ma-demo`）

```
$ media-agent init          # 1回目。終了コード 0
created  .media-agent/config.yaml
created  .media-agent/strategy.md
created  .media-agent/rules.md
created  .media-agent/agents/.gitkeep
created  .media-agent/memory/.gitkeep
created  .media-agent/data/
created  .media-agent/logs/
created  .media-agent/.gitignore
Media Agent を初期化しました: /tmp/ma-demo

$ media-agent init          # 2回目（冪等）。終了コード 0。8行すべて skipped
$ head -8 .media-agent/config.yaml | tail -2
project:
  name: "ma-demo"           # ディレクトリ名（詳細設計 12.3）
$ cat .media-agent/.gitignore
data/ / logs/ / .env / .env.*   （詳細設計 12.2 のとおり）
```

`data/media-agent.db` は生成していない（順序制約 O-1。T-005 が 12.4 の1行を足す）。

### 完了条件5・6: 終了コード（詳細設計 17.2）

```
$ media-agent -C /tmp/ma-empty status
Error: Media Agent が初期化されていません: /tmp/ma-empty
Hint: media-agent init を実行してプロジェクトを初期化してください
rc=3

$ media-agent post
Error: `media-agent post` は Stage 0 では未実装です（Stage 3 で実装予定）
  - X Connector が Stage 3 で入る
rc=10

$ media-agent -C /tmp/ma-demo status     # project.name 欠落 + actions.post.mode: autoo
Error: config.yaml の検証に失敗しました (2 件): /tmp/ma-demo/.media-agent/config.yaml
  - project.name: 必須項目がありません
  - actions.post.mode: 'autoo' は使用できません（auto / approval / disabled のいずれか）
Hint: media-agent doctor を実行すると、設定の問題をまとめて確認できます
rc=4
```

### 完了条件7: 受け入れテスト（S-A / S-D / S-I）

```
$ pytest tests/acceptance/test_stage0_init.py tests/acceptance/test_stage0_uninitialized.py \
         tests/acceptance/test_stage0_config_errors.py tests/acceptance/test_stage0_entrypoint.py \
         tests/acceptance/test_network_guard.py -q
7 failed, 52 passed in 0.98s
```

- **S-A（`test_stage0_init.py`）8件すべて通過**
- **S-D（`test_stage0_uninitialized.py`）17件すべて通過**
- S-I（`test_stage0_config_errors.py`）21件中 14件通過。**落ちている7件はすべて T-007 の範囲**
  （`doctor` と `agent list` の本体）:

| テスト | 落ちている理由 |
| --- | --- |
| `test_doctor_reports_config_problems_as_failed_checks[yaml-syntax]` | `doctor` の15検査（詳細設計 13.2）が未実装。T-007 |
| `test_doctor_reports_config_problems_as_failed_checks[missing-required-key]` | 同上 |
| `test_doctor_reports_config_problems_as_failed_checks[unknown-key]` | 同上 |
| `test_doctor_reports_config_problems_as_failed_checks[invalid-enum]` | 同上 |
| `test_doctor_reports_config_problems_as_failed_checks[unsupported-version]` | 同上 |
| `test_doctor_reports_config_problems_as_failed_checks[missing-file]` | 同上 |
| `test_agent_list_works_but_task_list_fails_on_broken_config` | `agent list` の本体（Registry の一覧出力）が未実装。T-006 の Registry と T-007 |

`test_stage0_entrypoint.py`（entry point の疎通）と `test_network_guard.py` も通過している。
**entry point のテストは `PATH` に `.venv/bin` が入っている状態（仮想環境を有効化した状態）で実行すること。**
`shutil.which("media-agent")` を使うため、`.venv/bin/pytest` と前置しただけでは console script を見つけられない。

### 全実装タスク共通の完了条件: テスト・リント

```
$ pytest tests/unit -q
115 passed in 0.44s

$ pytest -q          # 正典（引数なし）
ERROR tests/acceptance/test_stage0_agent_runtime.py
ERROR tests/acceptance/test_stage0_audit.py
ERROR tests/acceptance/test_stage0_policy.py
ERROR tests/acceptance/test_stage0_task_lifecycle.py
!!!!! Interrupted: 4 errors during collection !!!!!
```

**`pytest`（引数なし）は現時点では通らない。** 上記4モジュールが
`media_agent.core.task` / `core.runtime` / `core.policy` / `agents.builtin`（T-006）と
`core.db` / `core.observability`（T-005）を import しており、収集時に `ModuleNotFoundError` になる。
収集エラーが1件でもあると pytest は実行前に中断するため、**T-005・T-006 が入るまでこの状態が続く。**
`--continue-on-collection-errors` を既定に足すことはしなかった（本物の収集エラーを隠すため）。

収集できるものだけを実行した結果:

```
$ pytest --ignore=tests/acceptance/test_stage0_agent_runtime.py \
         --ignore=tests/acceptance/test_stage0_audit.py \
         --ignore=tests/acceptance/test_stage0_policy.py \
         --ignore=tests/acceptance/test_stage0_task_lifecycle.py -q
20 failed, 167 passed in 1.42s
```

落ちている20件の内訳: 上記 S-I の7件 + `test_stage0_doctor.py` 7件（T-007）+ `test_stage0_status.py` 6件（T-007）。

```
$ ruff check .
All checks passed!          # rc=0
$ ruff format --check .
48 files already formatted  # rc=0
$ mypy src
Success: no issues found in 22 source files   # rc=0
```

### 品質基準 Q7（`core/` に `.media-agent` が出現しない）

```
$ grep -rn "\.media-agent" src/media_agent/core/ | wc -l
0
$ grep -rl "\.media-agent" src/media_agent/
src/media_agent/project/__init__.py
src/media_agent/project/layout.py
src/media_agent/project/scaffold.py
```
