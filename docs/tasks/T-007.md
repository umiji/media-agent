# T-007: doctor/status/run統合とCI整備

## メタデータ
- 作成日: 2026-08-23
- 更新日: 2026-08-24
- 状態: 実装中
- 優先度: 高
- 担当: org-implementation
- 進捗: T-006 完了（20ebb7e）を受けて org-implementation へ割り当てた
- 依存: T-006（ブロッカー）— doctor/status/run が報告する対象（Config・DB・Agent・Task・Policy）が揃っている必要があるため
- TDD適用可否: 適用（T-003 の受け入れテスト S-B / S-C を通し、S-A〜S-I の全てを通すことが目標）
- 手戻り回数: 0

## 目的

要件定義書18節 Stage 0 の検証条件
「`media-agent init` → Project生成 → `media-agent doctor` → `media-agent status` が正常動作すること」
を実際に満たす。**このタスクの完了が、今回のゴール達成条件 A-2 の充足である。**

あわせて Test 基盤を仕上げ、GitHub Actions でテストが回る状態にする。
要件定義書28節「最初に決定する対象」の Test構造・GitHub Actions を、設計から実装へ落とす工程である。

## 変更範囲

- `media-agent doctor` の実装（T-002 が定めた検証項目）
- `media-agent status` の実装（T-002 が定めた出力内容）
- `media-agent run` の実装（Stage 0 の時点での挙動は T-002 の設計に従う）
- Test 基盤の仕上げ（T-001 の Test 構造。テスト実行コマンド、カバレッジ設定などがあれば）
- GitHub Actions workflow（`.github/workflows/` 配下。**テストとリントを実行するだけのもの**）
- 上記に対応する単体テスト
- 本ファイル `docs/tasks/T-007.md` の 決定ログ / 作業ログ / 証拠 / 進捗

## 完了条件

1. `media-agent doctor` が T-002 の検証項目を全て実行し、`init` 済みプロジェクトで**終了コード 0** になる
   （**受け入れテスト S-B**）
2. **`doctor` が、認証情報が無いことを異常として報告しない。**
   Stage 0 には認証情報を必要とする機能が存在しないため（PO 制約 C-2、要件定義書14節）
3. `media-agent status` が現在の Agent / Task の状態を出力し、終了コード 0 になる（**受け入れテスト S-C**）
4. `media-agent run` が T-002 の設計どおりに動作する
4-c. **`--json` が `run` / `agent list` / `task list` でも使える**（T-002 の範囲追記。詳細設計 15.3。
   方式設計 6.2 は `status` のみを対象としていたが、テストが出力文言に依存しないために拡張された）
4-b. **`media-agent agent list` と `media-agent task list` が動作する**（T-001 の差分 D-2。
   方式設計 6.4 で Stage 0 の実装対象と決まった。いずれも**読み取り専用**。
   Runtime と Task を CLI という公開された面から観測できるようにするためであり、
   受け入れテストが内部 API を直接叩く形になるのを避ける）
5. **受け入れテスト S-A 〜 S-I が全て通る。** 落ちているものが1つも無いこと。
   **S-H（Audit）を含む。** S-H の判定は T-005 の完了条件だったが、S-H が `media-agent run`（本タスク）と
   `PolicyRequest`（T-006）を経由するため、2026-08-24 にオーケストレーターが本タスクへ移した
   （経緯は `docs/tasks/T-005.md` の決定ログ）
5-b. **`pytest`（引数なし＝正典のコマンド）が通る。** 本タスクは、収集エラーで中断しない状態になる
   最初のタスクである（T-006 完了で収集は通り、本タスクで全件が緑になる見込み）
6. GitHub Actions の workflow が存在し、push で**テストとリントが実行される**構成になっている
   （実際の CI 実行結果の確認までは求めない。workflow ファイルの妥当性まで）
7. 要件定義書17節の Logging / Audit 要件どおり、`run` の実行が追跡可能な形で記録される
8. **リポジトリのルートに、T-001 が定めたコマンドで実際にテストとリントが通る**

### 全実装タスク共通の完了条件

上記に加えて、次を全て満たすこと。

- **自分が書いたコードに単体テストがある**（受け入れテストは T-003 の担当。単体テストは実装の担当）
- T-001 が定めたテスト実行コマンドとリントコマンドが**通る**
- **担当範囲の受け入れテストが通る。** 他タスクの範囲に依存して落ちているものは、
  どのテストがなぜ落ちているかを `## 証拠` に明記して残す（そのまま次タスクへ引き継がれる）
- 論理的なまとまりごとに commit し、**タスク完了時に `claude/session-resume-mdqrmg` へ push している**
  （レビュー通過を待たない。`CLAUDE.md` の Git 規約）
- `## 証拠` に、テスト実行コマンドと**実出力**、コミット SHA が記録されている

### T-003 からの申し送り（2026-08-23・オーケストレーター追記）

受け入れテストは **`tests/acceptance/` に既に存在し、全件落ちている**（コミット `7277a27`）。
**テストを書き換えて通してはならない**（禁止事項）。したがって、T-003 が設計の記述だけでは決まらず
**自分で決めた次の期待値は、実装側が合わせるべき契約**である。設計と矛盾すると判断した場合は、
直さずに停止条件として報告すること。

| # | 契約 | 根拠（T-003 の判断） |
| --- | --- | --- |
| K-1 | `media_agent.core.task` / `core.runtime` / `core.policy` / `agents.builtin` の主な公開物を、**`__init__.py` で再エクスポートする**（`from media_agent.core.task import TaskService` の形で import できること） | 詳細設計4章が「モジュール＝ディレクトリ、主な公開物＝その型」と対応付けている |
| K-2 | Repository のコンストラクタは `TaskRepository(conn)` / `DecisionRepository(conn)`（**位置引数**で `sqlite3.Connection`） | 詳細設計 6.6「すべて `sqlite3.Connection` を受け取り」。`__init__` の明示が無い |
| K-3 | `run --json` は**失敗時（終了コード1）にも** stdout へ JSON を出す | 15.3 の JSON に `error` キーがある以上、失敗時に出ないと `error` が無意味 |
| K-4 | 未初期化時の `doctor` / `status` / `run` / `agent list` / `task list` は、**stdout が空**（エラーは stderr） | 19章「stdout に成功を示す出力を出さない」 |
| K-5 | `run` の結果は**別プロセス（別接続）からも見える**。すなわちコミットが要る | 14章の出力例が実行済み Task の集計を示している |
| K-6 | `doctor --json` の `project_root` は `resolve()` 後のパスと比較できること | 絶対パスとしか書かれていないため、symlink 差で落ちないようにした |

### T-004 からの接続点（2026-08-24・オーケストレーター追記）

| # | 内容 |
| --- | --- |
| J-8 | **置き換え対象は `src/media_agent/cli/commands/{doctor,status,run,agent,task}.py` の `not_implemented_yet(...)` 5か所。** T-004 がプロジェクト解決と Config 読み込みまでを済ませ、本体だけを空けてある |
| J-9 | **`not_implemented_yet`（終了コード 70）と、スタブコマンド（終了コード 10）は別物である。** 前者は「Stage 0 で実装するが、まだ実装タスクが来ていない」状態。置き換え漏れが 70 として観測できるようにしてある |
| J-10 | オプション（`--json` / `--agent` / `--input` / `--status` / `--limit`）は設計どおり登録済み。**`--status` の値域検証（不正なら終了コード 2）は未実装**であり、本タスクの担当 |
| J-11 | K-3（`run --json` は失敗時にも JSON を出す）・K-6（`doctor --json` の `project_root` は解決済みパス）は**本タスクの担当**。K-6 の下地として `layout_for()` が `root` を `resolve()` して保持している |
| J-12 | S-I のうち7件が本タスク待ちで落ちている。`doctor` の15検査（詳細設計 13.2）が未実装のため6件、`agent list` の出力で1件 |
| J-13 | ruff の設定で `docs/` は対象外。**`ruff format` が Markdown 中の Python コードブロックを書き換えるため**。この除外を外さないこと |
| J-14 | **`test_stage0_entrypoint.py::test_console_script_reports_the_package_version` は、仮想環境を有効化した状態（`.venv/bin` が PATH にある状態）で実行する必要がある。** `.venv/bin/pytest` と前置しただけでは落ちる。実装の不備ではない |

### T-005 からの接続点（2026-08-24・オーケストレーター追記）

| # | 内容 |
| --- | --- |
| L-7 | **`setup_logging()` を呼ぶ箇所が製品コードに存在しない。本タスクの担当とする。** 実装済みだが CLI から呼ばれておらず、`logs/media-agent.log` が生成されない。**S-H の `log_path(...).is_file()` はこれが無いと落ちる。** 呼ぶ形は `setup_logging(level=config.logging.level, log_path=layout.log_path, verbose=cli_ctx.verbose, quiet=cli_ctx.quiet)`（プロジェクト解決後。罠 D-T16） |
| L-8 | **`doctor` は `open_project_db(layout, config)` に `config` を渡さないこと**（副作用禁止・詳細設計 13.1）。DB を作らせないなら `open_project_db` 自体を使わず読み取り専用に開く必要がある（`ensure_schema` も走るため） |
| L-9 | **S-H の `test_audit_is_not_suppressed_by_log_level` は、1回の `run` 後に監査記録がちょうど1件であることを要求する。** `run` が Policy Check を呼ばない（詳細設計 15.4）ことと整合するが、**余分に記録すると落ちる** |
| L-10 | `init` の出力に9行目 `created  .media-agent/data/media-agent.db` が増えた（T-005 が接続点 J-1 を入れたため） |

### T-006 からの接続点（2026-08-24・オーケストレーター追記）

T-006 は完了済み（コミット `ad9b2cf` / `3110997` / `39278d5`、記録 `20ebb7e`）。
Task・Runtime・Policy・組み込み Agent 2種はライブラリとして完成している。
**残り33件の受け入れテストは、すべて CLI 本体が `not_implemented_yet`（終了コード 70）で終わることによる。
本タスクがそれを実装すれば全件通る見込みである。**

| # | 内容 |
| --- | --- |
| M-1 | `AgentRunner.run(agent_name, payload, *, task_type="agent_run") -> TaskRunResult(task, output, error)`。**失敗時も例外を投げない。** 終了コード1（`AgentExecutionFailedError`）への変換は CLI 側で `result.task.status` を見て行う（設計 15.1） |
| M-2 | `run --json` に要る項目は `TaskRunResult.task`（`task_id` / `status` / `created_at` / `started_at` / `completed_at`）と `output.payload` / `error` からすべて取れる。**失敗時も stdout へ JSON を出すこと**（契約 K-3） |
| M-3 | `agent list` は `build_default_registry().all()` の `name` / `version` / `description`（昇順は保証済み）。**Config を読まない** |
| M-4 | `task list` は `TaskService.list(status=..., limit=...)`。`status` の集計は `TaskService.counts()`（5状態すべてのキーを返す） |
| M-5 | `doctor` の `policy.config` 検査は **`PolicyEngine.check(..., record=False)` を使うこと**（罠 D-T14）。`record=True` だと診断そのものが上限を消費する |
| M-6 | `run` は Policy Check を呼ばない（設計 15.4）。**Policy を経由しない Action 実行経路を作らないこと**とは別の話である。Stage 0 に Action は存在しない |
| M-7 | `TaskService` は commit しない。`AgentRunner` の「遷移 → 監査記録」順により `DecisionRepository.add` の commit（L-2）で Task も確定する（契約 K-5 を満たす）。**この順序に依存しているので、CLI 側で確定を追加しないこと** |

**T-006 が設計に無い箇所を自分で決めた事項が9件ある**（`docs/tasks/T-006.md` の決定ログ）。
T-009 のレビュー対象であり、本タスクはそれを**前提として扱ってよい**（覆った場合は修正タスクを起こす）。

## 判断してよい範囲

- `doctor` / `status` の出力の見た目（設計に指定が無い範囲で）
- GitHub Actions の Python バージョンマトリクス、キャッシュ設定
- モジュール分割・命名、単体テストの粒度

## 禁止事項

- **受け入れテスト（T-003 が書いたテスト）を書き換えて通さない。** テストが誤っていると判断したら、
  直さずに停止条件として報告する。`.claude/rules/org-task-ledger.md`「やってはいけないこと」に該当する
- 設計成果物（`docs/design/` 配下）を書き換えない。読み方に迷ったら org-design へ問い合わせる（1往復まで）
- `docs/requirements-media-agent-v0.2.md` / `docs/task-list-media-agent.csv` / `docs/handover.md` /
  `docs/po-queue.md` / 他タスクの `docs/tasks/T-XXX.md` を書き換えない
- **X API クライアントを書かない。外部ネットワークへ送信するコードを書かない**（PO 制約 C-1・C-3、
  および要件定義書28節「Stage 0ではまだX APIやニュース取得等を実装しない」）
- **認証情報を必要とする実装をしない。** 認証情報をリポジトリへ置かない（PO 制約 C-2、要件定義書14.2節）
- **Stage 1 以降の機能を実装しない。** Content / Research / Strategy / Analytics / Engagement / Orchestrator の
  各 Agent、Google News / RSS / Web Connector、過去投稿 Import、重複チェック、AI 呼び出しは範囲外
- **本タスクの `変更範囲` の外にあるファイルを変更しない。** 他の実装タスクが担当する領域に不足を見つけても、
  その場で直さずに報告する（`CLAUDE.md`「スコープを勝手に広げない」）
- スコープ外のリファクタリング・警告修正をしない
- **GitHub Actions に、外部サービスへ接続する job・認証情報を要求する job を書かない**（PO 制約 C-1・C-2）
- **Scheduler（cron 実行）を実装しない。** 要件定義書19節の表で GitHub Actions は S3 から。
  Stage 0 で作るのは「テストを回す CI」であり、定期実行の仕組みではない

## テスト方法

- 単体テスト: doctor の各検証項目、status の出力生成、run の制御
- 受け入れテスト: T-003 が書いた S-A 〜 S-I を**全て**実行する
- 実環境確認: **要**。一時ディレクトリで `init` → `doctor` → `status` → `run` を実際に順番に実行し、
  実出力を `## 証拠` へ記録する。実施者は担当自身
- 回帰: T-004 / T-005 / T-006 の受け入れテストが引き続き通ること

## 停止条件

共通の停止条件に加えて:

- 受け入れテストのうち、**設計どおりに実装しても通らないもの**がある
- `doctor` の検証項目に、認証情報または外部接続を必須とするものが含まれている
- Stage 0 の範囲で `run` が何をすべきか、T-002 の設計から一意に定まらない

## 参照すべき成果物

| パス | 何が書いてあるか |
| --- | --- |
| `docs/requirements-media-agent-v0.2.md` | 要件定義書 v0.2 全文。**18節 Stage 0・19節の対応表・28節を必ず読むこと** |
| `docs/design/stage0-architecture.md` | T-001 の方式設計成果物（構造・技術選定・実行コマンド） |
| `docs/design/stage0-detail.md` | T-002 の詳細設計成果物（Config / DB / Agent Interface / Task / Policy / Logging・Audit） |
| `docs/handover.md` | 今回のゴール達成条件 A-1〜A-8、PO 制約 C-1〜C-4 |
| `CLAUDE.md` | AI開発組織の運営規約 |
| `.claude/rules/org-task-ledger.md` | 決定ログ / 作業ログ / 証拠 の書き方 |
| `.claude/rules/org-escalation.md` | 共通の停止条件とエスカレーション手順 |
| `docs/glossary.md` | この組織で意味が決まっている語 |

**PO 制約（今回のゴール全体に効く）**:

- C-1 X への実際の投稿を行わない
- C-2 認証情報は環境に置かれていない。取得しようとしない
- C-3 投稿処理は、ダミーの送信先か、記録するだけの実装に対して開発する
- C-4 実際の投稿を試す必要が出たら、作業を止めてオーケストレーターへ報告する

**Stage 0 の範囲では投稿処理そのものが存在しない。** 要件定義書28節が「Stage 0 ではまだ X API や
ニュース取得等を実装しない」と明記しているため、ダミー送信先すら作らない。

なお、`docs/design/` 配下の実際のファイル名は T-001 / T-002 の成果物に従うこと。上表は想定である。

## 成果物

（完了時に担当が記入する。ファイルパスとコミット SHA で書くこと）

## ブロッカー

無し（T-006 は 2026-08-24 に `完了`。Task・Runtime・Policy・組み込み Agent は実装済み）。

## 決定ログ

## 作業ログ

## 証拠
