# T-009: Stage 0 レビュー

## メタデータ
- 作成日: 2026-08-23
- 更新日: 2026-08-24
- 状態: レビュー中
- 優先度: 高
- 担当: org-review
- 進捗: 1周目で Major 1件（RV-1）。T-014 完了（d642000）を受けて2周目（再判定）へ
- 依存: T-008（ブロッカー）— 実装とテストが揃っていないとレビューできないため／T-014（ブロッカー）— **再判定のため**。RV-1 の修正が入るまで完了条件3（Blocker / Major が0件）を満たせない
- TDD適用可否: 適用外（レビュー工程であり、事前にテストへ変換できない）
- 手戻り回数: 0（RV-1 は実装の誤りではなく設計の空白のため、手戻りに数えない）

## 目的

Stage 0 の成果物が、要件・設計・品質基準を満たしているかを独立に確認する。
**ゴール達成条件 A-5（Blocker / Major が残っていない）の判定がこのタスクである。**

実装エージェントもテストエージェントも、自分が作ったものの前提を疑いにくい。
第三の目で、要件との対応・保守性・見落とされたリスクを確認する。

## 変更範囲

- レビューの実施と、指摘の記録
- 本ファイル `docs/tasks/T-009.md` の 決定ログ / 作業ログ / 証拠 / 進捗

## 禁止事項

- **自分では修正しない**（`CLAUDE.md` の組織構成。レビューは修正しない）
- 製品コード・テストコード・設計成果物を書き換えない
- `docs/task-list-media-agent.csv` / `docs/handover.md` / `docs/po-queue.md` / 他タスクの `T-XXX.md` を書き換えない
- **Stage 1 以降の設計・実装について指摘しない。** 今回の範囲は Stage 0 である。
  将来のための示唆は Minor として1箇所にまとめること

## 完了条件

1. 次の観点で全てレビューし、指摘に **Blocker / Major / Minor** の重大度が付いている
   - **要件との対応** — 要件定義書18節 Stage 0 の実装項目が全て存在するか（Python Package / CLI /
     Project初期化 / `.media-agent/` / Config / DB / Logging / Audit / Task / Agent Interface /
     Policy基盤 / Test基盤）
   - **設計との整合** — 実装が `docs/design/` 配下の設計成果物どおりか。差異があれば、
     設計と実装のどちらを正とすべきかの意見を添える
   - **非機能要件** — 要件定義書20節の6項目（拡張性 / 保守性 / 可観測性 / 安全性 / 再現性 / 移植性）。
     特に **20.2 CoreとProject固有ロジックの分離**（開発原則6）と
     **20.4 DecisionとActionの分離**（開発原則5）は Stage 0 の設計思想の核なので必ず見ること
   - **保守性** — Stage 1 以降で Agent / Connector を追加する際に、Core を書き換えずに済む構造か
   - **明らかなバグ・リスク** — 例外処理の漏れ、ファイル破壊、冪等でない初期化
   - **セキュリティ** — 要件定義書14節。Credential がリポジトリに入っていないか、`.gitignore` が効いているか
2. **PO 制約の遵守を確認している**（ゴール達成条件 A-8）
   - X API クライアント・外部ネットワーク送信のコードが**無い**
   - 認証情報がリポジトリに**無い**
   - Stage 1 以降の機能（Content / Research / Strategy / Analytics / Engagement / Orchestrator、
     Google News / RSS / Web Connector）が実装されて**いない**
   - 確認に使ったコマンドと結果を `## 証拠` へ記録すること
3. **Blocker / Major の指摘が0件である。** 0件でない場合は org-implementation へ修正要求を返す（2往復まで）。
   2往復で収束しなければ、収束していなくてもオーケストレーターへ返す
4. Minor の指摘は、修正するかどうかの判断をオーケストレーターへ委ね、一覧として報告に含める
5. **T-004 が ruff の `select` から E501（行長）を外した判断を確認する。**
   理由は「T-003 のテスト docstring が101件違反し、`ruff format` では解消できず、
   テストの文面書き換えは禁止事項だったため」。**品質基準 Q2 の実効性が下がっていないか**を見ること。
   妥当と判断するなら、その旨を報告に書く（オーケストレーターが層2へ反映する）
6. **T-003 が設計の記述だけでは決まらず自分で決めた期待値 K-1〜K-6 が、設計成果物と矛盾していないかを確認する。**
   一覧は `docs/tasks/T-004.md` の「T-003 からの申し送り」にある。
   TDD では受け入れテストが実装より先に固まるため、テストが設計の穴を独自解釈で埋めた箇所は
   **誰も照合しないまま契約になる。** ここを見るのはレビューだけである
7. `docs/glossary.md` に無い語で、設計・実装が新たに定義した語があれば、報告で申告する（登録はオーケストレーターが行う）

### T-008 からの接続点とレビュー観点（2026-08-24・オーケストレーター追記）

T-008 は完了済み（コミット `b2f1fc7` / `3e7117d`）。**判定は合格、Blocker / Major は 0 件。**
受け入れテストは **538 passed**（単体 404 / 受け入れ 134。S-J / S-K が追加された）。

| # | 内容 |
| --- | --- |
| P-1 | **下表 R-1 / R-2 は「設計どおりだが利用者体験として妥当か」という同じ類型である。並べて判断すること** |
| P-2 | **F-2（受け入れテストの DB 接続リーク）は org-test の成果物の欠陥であり、実装のレビュー対象ではない。** 別タスク **T-011** として登録済み。**本タスクで指摘し直さないこと** |
| P-3 | S-J に、T-003 の「`os.chdir` を使わない」という申し合わせからの**意図的な逸脱が1件**ある（`test_nested_project_resolves_to_the_nearest_root`）。理由は `docs/tasks/T-008.md` の決定ログ。**逸脱の妥当性はレビュー対象** |
| P-4 | 品質基準 Q6 は遮断 fixture に加え**ソケット0個の実測**で、Q7 は grep で裏付け済み（`docs/tasks/T-008.md` の証拠 E-5）。**再実行の必要は薄い** |
| P-5 | A-2（実環境での `init` → `doctor` → `status`）は **org-test が独立に確定させた**（E-2）。A-4 は 3.11 / 3.12 / 3.13 で確定。**残る未確定は A-5（本タスク）と A-6（T-010）だけである** |
| P-6 | **X-2（GitHub Actions の実行）は解消した。** オーケストレーターが実行結果を観測した（run #3 / `3e7117d`、matrix 3ジョブすべて success）。**サブエージェントは GitHub へアクセスできない。観測はオーケストレーターの担当である** |

#### 判断を要する観点（オーケストレーターが集約したもの）

| # | 観点 | 出所 |
| --- | --- | --- |
| R-1 | `media-agent run --agent fail` が stderr にトレースバックを出す。**設計どおりだが**、`app.py` が内部エラーのトレースバックを `--verbose` 時だけ出す方針と非対称である | T-007 の申告（`core/runtime/runner.py:166`） |
| R-2 | 構造が壊れたプロジェクト（DB が SQLite でない、`logs/` や `data/` が通常ファイル）で `status` / `run` が**終了コード 70「内部エラー」**になる。同じ状況を `doctor` は終了コード 5 で正確に診断する。`sqlite3.DatabaseError` / `OSError` が `media_agent.errors` へ包まれず素通りしている（`core/db/connection.py:51-52`）。**同じ DB 由来でもスキーマ版数不一致は正しく包まれる**ため扱いが非対称。ただし**詳細設計 17.2 はこの状況の期待値を定めていない**（設計の空白） | T-008 の F-1 |

**R-1 / R-2 は、いずれも「実装が設計に違反している」ものではない。** 判定してほしいのは
**(a) このまま Stage 0 を完了としてよいか、(b) 修正タスクを起こすべきか、(c) 設計側の欠落として
org-design へ差し戻すべきか** である。Blocker / Major / Minor の重大度を付して返すこと。

### 2周目（再判定）で見るもの（2026-08-25・オーケストレーター追記）

T-012 → T-013 → T-014 が完了し、**RV-1 の修正が入った**。**本タスクの残りは再判定だけである。**

| # | 内容 |
| --- | --- |
| V-1 | **1周目の指摘を全件たどり直すのではなく、「Blocker / Major が残っていないか」を判定すること。** Minor の処理は既に決着している（下表） |
| V-2 | **RV-1 が解消したかを実地で確認すること。** 判定の要点は「**1 であって 70 でないこと**」と、**`doctor` の助言に従った利用者が行き止まりに入らないこと**（`doctor`=5 → 退避 → `init`=0 → `doctor`=0） |
| V-3 | **T-014 が設計に無い箇所を自分で決めた事項が5件ある**（`docs/tasks/T-014.md` の決定ログ）。**これが2周目の主な新規レビュー対象である** |
| V-4 | **T-012（設計）が新たに書いた 17.4 / 16.2 / 11.3 / 19.1 も対象である。** 1周目には存在しなかった |
| V-5 | 基準値: `PATH="$PWD/.venv/bin:$PATH" .venv/bin/pytest -q` → **679 passed**。`ruff check` / `ruff format --check` / `mypy src` すべて 0。CI は run #14（`d642000`）で success（**オーケストレーターが観測済み。あなたは GitHub へアクセスできない**） |
| V-6 | **手を出さないもの**: RV-2（`run --agent fail` の stderr。1周目で (a) と決着済み）／F-2 と RV-7（T-011 / 記録のみ）／`S-J` `S-K` の記号重複（**T-015** の担当）／文書のスキーマ版数の追随（**T-016** の担当） |

**1周目の Minor 6件は、すべてオーケストレーターが処理を決定済みである**（`docs/handover.md`）。
**蒸し返さないこと。** 新たに見つかったものだけを挙げること。

## 判断してよい範囲

- 指摘の重大度の判定
- レビューの深さと順序
- 「設計と実装のどちらを正とすべきか」の意見表明（決定はオーケストレーター）

## テスト方法

レビュー工程のため自動テストは無い。テストの実行結果は T-008 の `## 証拠` を参照すること。
**テストを自分で実行し直してもよい**（読み取りと実行のみ。コードは直さない）。

## 停止条件

共通の停止条件に加えて:

- Blocker が **org-implementation との2往復で解消しない**
- 設計成果物と実装が広範に食い違っており、どちらを正とすべきか組織内で決まらない
- **PO 制約への違反を発見した**（外部送信コード、認証情報の混入など）。直ちに止めて報告する

## 参照すべき成果物

| パス | 何が書いてあるか |
| --- | --- |
| `docs/requirements-media-agent-v0.2.md` | 要件定義書 v0.2 全文。**18節 Stage 0・19節の対応表・28節を必ず読むこと** |
| `docs/design/stage0-architecture.md` | T-001 の方式設計成果物（構造・技術選定・実行コマンド） |
| `docs/design/stage0-detail.md` | T-002 の詳細設計成果物（Config / DB / Agent Interface / Task / Policy / Logging・Audit） |
| `docs/handover.md` | 今回のゴール達成条件 A-1〜A-8、PO 制約 C-1〜C-4 |
| `CLAUDE.md` | AI開発組織の運営規約 |
| `.claude/rules/org-task-ledger.md` | 決定ログ / 作業ログ / 証拠 の書き方 |
| `.claude/rules/org-escalation.md` | 共通の停止条件とエスカレーション手順 |
| `docs/glossary.md` | この組織で意味が決まっている語 |

**PO 制約（今回のゴール全体に効く）**:

- C-1 X への実際の投稿を行わない
- C-2 認証情報は環境に置かれていない。取得しようとしない
- C-3 投稿処理は、ダミーの送信先か、記録するだけの実装に対して開発する
- C-4 実際の投稿を試す必要が出たら、作業を止めてオーケストレーターへ報告する

**Stage 0 の範囲では投稿処理そのものが存在しない。** 要件定義書28節が「Stage 0 ではまだ X API や
ニュース取得等を実装しない」と明記しているため、ダミー送信先すら作らない。

なお、`docs/design/` 配下の実際のファイル名は T-001 / T-002 の成果物に従うこと。上表は想定である。
`docs/tasks/T-004.md` 〜 `docs/tasks/T-008.md` の `## 証拠` に、各工程の実行結果がある。

## 成果物

| 種類 | 内容 |
| --- | --- |
| レビュー結果 | 本ファイルの `## 証拠` と、オーケストレーターへの完了報告 |
| 指摘一覧 | 下記「指摘一覧」（Blocker 0 / Major 1 / Minor 6） |
| 判定 | **要修正**（Major 1件）。ただし差し戻し先は org-implementation ではなく **org-design**（E-3） |

### 指摘一覧

| ID | 重大度 | 対象 | 何が問題か | 期待する状態 |
| --- | --- | --- | --- | --- |
| **RV-1** | **Major** | `src/media_agent/core/db/connection.py:51-52` / `src/media_agent/project/scaffold.py:129` | 構造が壊れたプロジェクト（DB が SQLite でない、`data/` や `logs/` が通常ファイル）で `status` / `run` / `task list` **および `init`** が終了コード **70「想定外の内部エラー」**になる。`sqlite3.DatabaseError` / `OSError` が `media_agent.errors` へ包まれていない。**`doctor` は同じ状況を終了コード 5 で正確に診断し、`media-agent init を実行してください` と助言するが、その助言に従うと 70 で終わる**（利用者から見た復旧経路の行き止まり） | 環境側の異常が `MediaAgentError` の階層で表現され、`doctor` の助言と実際の挙動が一致すること。**先に詳細設計 17.2 がこの状況の期待値を定めること**（現状は定めが無く、実装は設計に違反していない） |
| RV-2 | Minor | `src/media_agent/core/runtime/runner.py:166-173` | `run --agent fail` が `--verbose` 無しでも stderr へトレースバックを出す（`-q` でも消えない。ERROR レベルのため）。`cli/app.py:49-52` が内部エラーのトレースバックを `--verbose` 時だけ出す方針と非対称 | 設計（11.1「運用ログはトレースバックを出す」/ 11.2「stderr 既定 WARNING 以上」）どおりであり Stage 0 では実害が無い。Stage 1 で Agent の失敗が日常になる前に、既定の詳細度を再検討できること |
| RV-3 | Minor | 詳細設計 11.3 / `src/media_agent/core/db/migrations.py:104-141` | **`Agent.version` がどこにも永続化されない。** `decisions` にも `tasks` にも列が無く、`AuditRecord` にも項目が無い。用途は `agent list` / `status` の表示だけ（`cli/rendering.py:99`）。要件20.5 は「同じ Input・設定・**Agent Version** から実行内容を追跡可能とする」と定め、詳細設計 8.1 も `version` を 20.5 の手段と説明しているが、記録側にその受け皿が無い | Agent 版数から過去の実行を特定できること。**設計の空白であり実装の欠陥ではない。** 版が 1 しか無い Stage 0 では観測できないが、後から足すと Migration 版数 2 と `AUDIT_SCHEMA_VERSION` の更新が要る |
| RV-4 | Minor | `docs/tasks/T-006.md` の `## 決定ログ` | 決定ログの項目は **3件**。`docs/handover.md:236` と `docs/tasks/T-007.md:124` は「T-006 が設計に無い箇所を自分で決めた事項が**9件**ある（T-006.md の決定ログ）」と記す。残り6件は台帳に存在せず、照合できない | 台帳の記述と実体が一致すること（件数の訂正、または残り6件の追記）。`.claude/rules/org-task-ledger.md`「記録は書いた時点で残す」 |
| RV-5 | Minor | `src/media_agent/project/templates/config.yaml:2` | 生成される `config.yaml` が `# 項目の意味: docs/design/stage0-detail.md 5章` を指す。このパスは **Media Agent 開発リポジトリの中にしか無く、利用者のプロジェクトには存在しない** | 利用者が到達できる参照先であること。T-010（README・利用者向け文書）の範囲と重なるため、担当の割り当てはオーケストレーターの判断 |
| RV-6 | Minor | `src/media_agent/cli/commands/task.py:33-36` | `--status` は値域を検査して `UsageError`（終了コード 2）にするが、`--limit` には下限検査が無い。`--limit -1` は SQLite の `LIMIT -1`（無制限）になり、`--limit 0` は行があるのに `(タスクはありません)` と出る。詳細設計 16.2 に定めは無い | 2つのオプションの扱いが揃っていること。実害は小さい |
| RV-7 | Minor | （将来への示唆・1箇所にまとめ。禁止事項の指示による） | Stage 1 以降で効くもの: (a) `tasks.input` / `output` は**マスクされない**（実測で確認。既知の Y-1）、(b) `PolicyRequest.task_id` の外部キー制約（既知の Y-2）、(c) Policy の上限が「allow した回数」の代理計数（既知の D-X1）、(d) 上方探索を検証する S-J の1ケースがプロセス全体の cwd を移すため、テストの並列実行（xdist 等）を導入すると順序依存になる | **Stage 0 の指摘ではない。** 記録のみ。(a)〜(c) は既に申し送り済みで、レビューとして再確認した結果「Stage 0 では露出しない」ことを実測で裏付けた |

**良い点**（維持すべき判断）

- 設計成果物への追随度が高い。`policy/engine.py` の判定順序、`errors.py` の階層と `exit_code`、`observability/audit.py` の書き込み順序は、詳細設計の表と1対1で対応している
- **品質基準 Q8 が構造で守られている。** 終了コードは例外のクラス属性が持ち、`cli/app.py` に `isinstance` の分岐表が無い
- **品質基準 Q9 が型で守られている。** `AgentContext` に DB 接続・Repository・`AuditRecorder` を渡さない設計により、Agent が永続化する経路が存在しない
- `core/` → `project/` の依存を `ProjectPaths` / `PolicyRequestLike` の構造的部分型で切っており、Q7 の grep が通るだけでなく静的依存としても無い
- `doctor` の副作用ゼロが徹底されている（`immutable=1` / `mode=ro` の使い分け、`policy.config` をメモリ DB で判定、`record=False`）

## ブロッカー

無し（T-014 は 2026-08-25 に `完了`。RV-1 の修正は入った。**2周目の再判定が本タスクの残りである**）。
1周目の指摘は下記の指摘一覧にある。
1周目のレビューそのものは完了しており、指摘は `docs/tasks/T-009.md` の指摘一覧にある。

## 決定ログ

### 2026-08-24 R-2（構造破損時の終了コード 70）を Major と判定し、差し戻し先を org-design とした
- 決定: 重大度 **Major**。対応は **(c) 設計の欠落として org-design へ差し戻す → その後 (b) 修正タスク**。
  Stage 0 の完了そのものは、オーケストレーターが別タスク化を決定すれば妨げない
- 根拠: 実装は詳細設計 17.2 に違反していない（17.2 にこの状況の行が無い）。したがって org-implementation へ
  差し戻しても「何を期待値とすべきか」が決まらず、2往復しても収束しない。エスカレーション条件 E-3 に当たる
- 却下案A: Minor とする → **レビューで新たに判明した事実が重みを変えた。** T-008 の F-1 は `status` / `run` の話だったが、
  実測すると `init` も 70 になる。`doctor` は同じ状況を 5 で診断したうえで `media-agent init を実行してください` と
  助言するため、**利用者はツール自身の助言に従って行き止まりに入る**。「動作に実害が無い」とは言えない
- 却下案B: Blocker とする → 完了条件を1つも満たさなくしておらず、破壊も情報漏洩も無い。
  `doctor` が原因と対象パスを正確に示すため、利用者が復旧できない状態ではない
- 出典: セッション内の判断。実測は本ファイル `## 証拠` の E-4。設計の空白は `docs/design/stage0-detail.md` 17.2

### 2026-08-24 R-1（Agent 失敗時のトレースバック）を Minor と判定し、(a) このまま完了とした
- 決定: 重大度 **Minor**。判定 **(a) このまま Stage 0 を完了としてよい**
- 根拠: 詳細設計 11.1 が「運用ログはトレースバックを出す」、11.2 が「stderr 既定 WARNING 以上」と定めており、
  `runner.py` の `exc_info` はその2つの帰結である。`app.py` の `--verbose` 条件は**想定外の内部エラー**に対する
  方針（方式設計 6.5）であって運用ログの方針ではない。**非対称は見かけであり、2つは別の規則に属する**
- 却下案: Major として `--verbose` 時のみに揃えさせる → 揃えると Agent の失敗原因が既定で運用ログに残らなくなる。
  要件20.3（可観測性）を、体験の一貫性のために下げることになる
- 付帯条件: Stage 1 以降で Agent の失敗が日常になると、既定でトレースバックが出る体験は変わる。
  そのときに再検討する（RV-2 として記録）
- 出典: セッション内の判断。実測は `## 証拠` の E-3

### 2026-08-24 ruff の E501 除外（T-004 の判断）を妥当と判定した
- 決定: **妥当。品質基準 Q2 の実効性は下がっていない**（完了条件5）
- 根拠: 実測すると、**文字数**が 88 を超える行は `src` に3行・`tests` に2行しかなく、最長 96 文字である。
  一方 E501 を再有効化すると 147 件（うち `src` 56 件）が出る。この差は ruff の E501 が
  **日本語などの全角文字を幅2として数える**ためであり、147 件はすべて日本語の説明文・docstring である。
  行の折り返しは `ruff format` が担保しており（`line-length = 88` は残っている）、
  除外によって失われたのは「日本語の散文の見た目の幅」だけである
- 却下案: E501 を戻して `noqa` で個別に抑制する → 147 箇所に `noqa` が付き、本物の長すぎる行を見つけにくくなる
- 出典: 本ファイル `## 証拠` の E-6。T-004 の判断は `docs/handover.md:190`

## 作業ログ

1. 2026-08-24 `docs/tasks/T-009.md` 全文と、接続点 P-1〜P-6・観点 R-1 / R-2 を読んだ。指示の充足を確認（完了条件・
   判断してよい範囲・参照すべき成果物がすべて実在）。着手時 `git status` はクリーン、HEAD は `33a6541`。共通の停止条件 S-1〜S-6 に該当なし
2. 要件定義書（5 / 8 / 10 / 14 / 16 / 17 / 18 / 19 / 20 / 26 / 28節）、方式設計（4 / 5 / 6 / 7 / 10 / 11 / 12 / 13 / 14章）、
   詳細設計（8 / 9 / 10 / 11 / 12 / 13 / 15 / 16 / 17章）を読み、実装と1件ずつ突き合わせた
3. `src/media_agent/` の全モジュール（26ファイル）を読んだ。品質基準 Q5〜Q10 を個別に確認
4. Q1〜Q4 を自分で再実行して確認（538 passed / ruff・mypy すべて終了コード 0）
5. PO 制約 C-1〜C-4 とゴール達成条件 A-8 を、import 一覧・grep・`git ls-files` で確認
6. 一時ディレクトリで実環境確認を行った: `init` → `doctor` → `status` → `run`（秘密値入り payload）→
   監査記録・運用ログ・DB の目視、`init` の冪等性と `--force` の範囲、R-1 / R-2 の再現、構造破損3パターン
7. K-1〜K-6 / T-006 の決定ログ / T-007 の決定ログ6件 / E501 の除外を、設計成果物と照合した
8. 本ファイルへ結果を記入し、`docs/tasks/T-009.md` だけを commit / push した（T-010 と並列のためパスを限定）

## 証拠

### E-1 品質基準 Q1〜Q4（自分で再実行）

```
$ PATH="$PWD/.venv/bin:$PATH" .venv/bin/pytest -q
538 passed in 11.70s          # TOTAL カバレッジ 98%
$ .venv/bin/ruff check .        -> All checks passed!        (exit 0)
$ .venv/bin/ruff format --check . -> 96 files already formatted (exit 0)
$ .venv/bin/mypy src            -> Success: no issues found in 49 source files (exit 0)
```

**オーケストレーターの基準値（538 passed / 3コマンドすべて 0）と一致した。**

### E-2 PO 制約 C-1〜C-4 とゴール達成条件 A-8（完了条件2）

```
$ grep -rhE "^\s*(import|from) " src/media_agent --include="*.py" | awk '{print $2}' | cut -d. -f1 | sort -u
__future__ abc click collections contextlib dataclasses datetime enum importlib json logging
media_agent pathlib pydantic re shutil sqlite3 subprocess sys tempfile traceback types typing uuid yaml
```

- **HTTP/SNS クライアントが1つも無い**（`requests` / `httpx` / `urllib` / `http.client` / `tweepy` いずれも不在）。
  `socket` は `src` に無く、`tests/conftest.py` の遮断 fixture にのみ現れる
- `subprocess` の用途は `cli/diagnostics.py:596-623` の**ローカル `git`** だけ（`ls-files` / `rev-parse`）。
  引数は固定で利用者入力を渡さない。ネットワークへ出るサブコマンドを使わない

```
$ git ls-files | grep -iE "\.env|credential|secret|\.pem|id_rsa|token"   -> 無し
$ grep -rn "\.media-agent" src/media_agent/core/ --include="*.py"          -> 出現なし   （Q7）
$ ls src/media_agent/
__init__.py __main__.py agents cli core errors.py project
```

- **`connectors/` が存在しない**（方式設計 5.1・拡張点 E-3 の「作らない」どおり）
- `agents/` の中身は `builtin/`（`echo` / `fail`）のみ。Content / Research / Strategy / Analytics /
  Engagement / Orchestrator は**不在**。Google News / RSS / Web も不在 → **A-8 を満たす**
- `.github/workflows/ci.yml` は Secrets を1つも参照せず、`permissions: contents: read`

### E-3 実環境確認（`init` → `doctor` → `status` → `run`。R-1 の再現を含む）

```
$ media-agent init    -> exit 0
$ media-agent doctor  -> exit 0
$ media-agent status  -> exit 0
$ media-agent run --agent echo --input '{"api_key":"s3cret-value","message":"hi"}'  -> exit 0
```

生成された `.media-agent/` は要件定義書8.1節と方式設計7章のとおり（`config.yaml` / `strategy.md` /
`rules.md` / `agents/.gitkeep` / `memory/.gitkeep` / `data/media-agent.db` / `logs/` / `.gitignore`）。

**品質基準 Q10（秘密値を出さない）を実測で確認した**:

```
audit.jsonl : "input": {"api_key": "***", "message": "hi"}   "result": {"api_key": "***", ...}   <- マスク済み
media-agent.log : agent=echo task=... started / completed elapsed=0.002s          <- 秘密値なし
tasks テーブル  : input/output は生の値                                            <- 既知の Y-1（設計 11.4 の対象外）
```

`task_as_json`（`cli/rendering.py:79-92`）は `input` / `output` を出さないため、**Stage 0 の CLI 出力に露出しない**。

R-1 の再現（`--verbose` 無し。`-q` でも同じ）:

```
$ media-agent run --agent fail            -> exit 1
ERROR: agent=fail task=... failed elapsed=0.002s error=AgentFailedForVerificationError: ...
Traceback (most recent call last):
  File ".../core/runtime/runner.py", line 107, in run
  ...
$ media-agent -q run --agent fail         -> exit 1（トレースバックは出たまま。ERROR レベルのため）
$ media-agent -v run --agent fail         -> exit 1
```

冪等性（S-K の再確認）— 利用者の編集・独自ファイル・Task 行がすべて残る:

```
$ （config.yaml を posts_per_day: 7 に編集し、agents/mine.md を置く）
$ media-agent init          -> exit 0、全項目 skipped、tasks 7 件のまま、posts_per_day: 7 のまま、mine.md 健在
$ media-agent init --force  -> exit 0、config.yaml は既定へ戻る／mine.md と tasks 7 件は無傷（詳細設計 12.5 どおり）
```

### E-4 R-2 の再現（3パターン。**`init` が 70 になることは新規に判明した事実**）

```
# (1) DB が SQLite でない
$ media-agent status     -> exit 70   Error: 内部エラーが発生しました: file is not a database
$ media-agent run        -> exit 70   （同上）
$ media-agent task list  -> exit 70   （同上）
$ media-agent doctor     -> exit 5    [fail] db.file  DB を SQLite として開けません: file is not a database
                                      hint = "media-agent init を実行してください（既存ファイルは変更されません）"
$ media-agent init       -> exit 70   Error: 内部エラーが発生しました: file is not a database   <- 助言の行き止まり

# (2) logs/ が通常ファイル      status=70 / doctor=5 / init=70   ([Errno 17] File exists)
# (3) data/ が通常ファイル      status=70 / doctor=5 / init=70   ([Errno 17] File exists)
```

`agent list` だけは Config も DB も読まないため影響を受けない。
発生箇所は `core/db/connection.py:51-52`（`mkdir` の `OSError` と `PRAGMA` 実行時の `sqlite3.DatabaseError`）と、
`project/scaffold.py:129`（`_ensure_database`）・`:187`（`_make_dir`）。

### E-5 K-1〜K-6（T-003 が自分で決めた期待値）と設計の照合（完了条件6）

| # | 期待値 | 照合結果 |
| --- | --- | --- |
| K-1 | `core.task` / `core.runtime` / `core.policy` / `agents.builtin` が主な公開物を再エクスポート | **矛盾なし。** 4つとも `__all__` を持ち、詳細設計4章の「モジュール＝ディレクトリ、主な公開物＝その型」と一致 |
| K-2 | Repository は `TaskRepository(conn)` の**位置引数** | **矛盾なし。** `repositories.py:76` が `def __init__(self, conn, *, clock=utcnow)`。詳細設計 6.6 と一致（`clock` はキーワード専用の追加であり、設計の記述と衝突しない） |
| K-3 | `run --json` は失敗時にも stdout へ JSON を出す | **矛盾なし。** `cli/commands/run.py:59-69` が出力してから例外を送出する。詳細設計 15.3 が `error` キーを定めている以上、この解釈でなければ `error` が無意味 |
| K-4 | 未初期化時は stdout が空 | **矛盾なし。** `open_session` / `project_root()` が出力前に例外を送出する。詳細設計19章と一致 |
| K-5 | `run` の結果が別接続から見える（コミットが要る） | **矛盾なし。** `DecisionRepository.add`（`repositories.py:395`）の commit が Task の更新も確定させる。詳細設計 11.3 手順1が明示的にコミットを要求しており、設計に反しない。**ただし Runner の「遷移 → 記録」の順序への依存**が生じており、T-006 が決定ログと単体テスト2件で固定している（妥当） |
| K-6 | `doctor --json` の `project_root` は `resolve()` 後 | **矛盾なし。** `layout_for`（`project/layout.py:51`）が `root.resolve()` を保持。設計は「絶対パス」としか書いておらず、より強い保証を選んでいる |

**6件とも設計成果物と矛盾しない。** K-2 / K-6 は設計より強い契約だが、設計の記述と衝突しない範囲での具体化である。

### E-6 ruff の E501 除外の妥当性（完了条件5）

```
$ .venv/bin/ruff check --select E501 --no-cache .        -> Found 147 errors.（うち src 56 件）
$ python3（文字数で計測） src: 88 文字超は 3 行（最長 96） / tests: 2 行（最長 90）
```

147 件と 5 行の差は、**ruff の E501 が全角文字を幅2として数える**ことによる。147 件はすべて日本語の
説明文・docstring であり、コードの行そのものは `ruff format`（`line-length = 88` は維持）が畳んでいる。
→ **Q2 の実効性は下がっていない。T-004 の判断は妥当**（決定ログに記録）。

### E-7 T-006 / T-007 の「設計に無い箇所を自分で決めた事項」（完了条件の観点「設計との整合」）

- **T-007 の6件**（`docs/tasks/T-007.md` の決定ログ）: **6件とも承認できる。**
  `doctor` がログファイルを作らない／読み取り専用接続（`immutable=1` と `mode=ro` の使い分け）／
  検査14 をメモリ DB で判定／検査3〜5 の依存を `skipped` にする／`-q` は結果出力を抑制しない／
  カバレッジに閾値を置かない。いずれも詳細設計 13.1・13.4・11.2、方式設計 3.5 の帰結であり、
  却下案も記録されている。**`doctor` の副作用ゼロを守るための判断が多く、設計思想に忠実である**
- **T-006**: 決定ログの項目は **3件**（Task の確定を `AuditRecorder` の commit に委ねる／Task 不在は
  `DatabaseError` を使う／`reason_code` から `outcome` を導出する）。**3件とも承認できる。**
  いずれも「新しい例外クラスや新しい責務を増やさない」方向の判断で、詳細設計 6.6 / 8.3 / 10.2 / 17.1 と整合する。
  **9件との差6件は台帳に存在せず照合できない**（RV-4）

### E-8 品質基準 Q5〜Q10（レビュー観点）

| # | 判定 | 根拠 |
| --- | --- | --- |
| Q5 | **満たす** | `src` の全モジュールに対応する `tests/unit/` がある（25ファイル）。専用ファイルを持たない `cli/context.py` / `cli/commands/*` / `core/db/models.py` / `__main__.py` も `test_cli_app.py` / `test_cli_session.py` / `test_cli_commands_stage0.py` / `test_task_models.py` / `test_stage0_entrypoint.py` が覆い、カバレッジ 98%（41ファイルが 100%） |
| Q6 | **満たす** | `tests/conftest.py:50-95` の autouse fixture が `connect` / `connect_ex` / `create_connection` / `getaddrinfo` を遮断。ループバックのみ許可。加えて E-2 の import 一覧に接続ライブラリが無い |
| Q7 | **満たす** | grep 出現なし。さらに `core/db/connection.py:33-42` の `ProjectPaths` と `observability/audit.py:58-94` の `PolicyRequestLike` により、**静的な import 依存としても** `core/` → `project/` が無い |
| Q8 | **満たす** | 終了コードは `errors.py` のクラス属性。`cli/app.py:39-53` が唯一の変換点で `isinstance` の分岐表が無い。**唯一の綻びが RV-1**（包まれない外部例外が 70 へ落ちる） |
| Q9 | **満たす** | `AgentContext`（`runtime/agent.py:70-81`）は `project_root` / `config` / `task_id` / `logger` のみ。DB 接続も `AuditRecorder` も渡らないため、**Agent が永続化する経路が型として存在しない**。Action は Stage 0 に存在せず、Policy は `run` から呼ばれない（詳細設計 15.4 どおり） |
| Q10 | **満たす** | E-3 の実測。監査記録・運用ログの両方でマスクを確認 |

### E-9 要件との対応（完了条件1）

要件定義書18節 Stage 0 の実装項目は**すべて存在する**:
Git Repository / Python Package（`pyproject.toml` + src レイアウト）/ CLI（`cli/`、10コマンド）/
Project初期化（`project/scaffold.py`）/ `.media-agent/`（E-3 で実測）/ Config（`core/config/`）/
DB（`core/db/`、要件16節の6 Entity）/ Logging（`observability/log.py`）/ Audit（`observability/audit.py`、
要件5.5節の9項目に1対1対応）/ Task（`core/task/`、詳細設計 9.2 の遷移表と完全一致）/
Agent Interface（`core/runtime/agent.py`）/ Policy基盤（`core/policy/`、詳細設計 10.4 の9段の判定順序と完全一致）/
Test基盤（`tests/`、538ケース + 遮断 fixture）。

要件定義書19節で S0 が **`設計`** の2項目（Custom Agent / Claude Code連携）は、方式設計9章・8章に設計だけがあり、
**実装は無い**（`init` は `.claude/` を読み書きせず、`agents/*.md` をロードしない）。**正しい。**

非機能要件20節:

| # | 判定 | 根拠 |
| --- | --- | --- |
| 20.1 拡張性 | 満たす | 拡張点 E-1〜E-9 が位置として用意され、Agent 追加時に `core/` を変更しなくてよい（`AgentRegistry` への登録のみ） |
| 20.2 保守性 | 満たす | Q7 に加え、`ProjectPaths` / `PolicyRequestLike` による構造的部分型で依存の向き（方式設計 5.2）を静的に守っている。**開発原則6を最も強く担保しているのがこの2つの Protocol である** |
| 20.3 可観測性 | 満たす | 監査記録は `logging.level` の影響を受けず、DB を正本・JSONL を写しとする二重化（詳細設計 11.1 の表と実装が一致） |
| 20.4 安全性 | 満たす | Policy Engine が判定のみを返し、Action を実行しない。`run` は Policy を呼ばない（詳細設計 15.4）。**Action が無い場所に関門を置かない**という判断が、開発原則5を形骸化させずに守っている |
| 20.5 再現性 | **一部** | `decisions` に input / decision / reason / timestamp が残り、DB / config のスキーマ版数も独立に保持される。**ただし Agent Version が永続化されない（RV-3）** |
| 20.6 移植性 | 満たす | 標準ライブラリ + 3依存のみ。CI が 3.11 / 3.12 / 3.13 の matrix。パス表示は `PurePosixPath` で正規化（`project/layout.py:36-42`） |

### E-10 P-3（S-J の `os.chdir` 逸脱）の妥当性

`tests/acceptance/test_stage0_project_isolation.py:238-269`。**逸脱は妥当と判定する。**

- 方式設計 10.3 が `os.chdir` を禁じた理由は「テスト間の順序依存を作らない」ことであり、
  `monkeypatch.chdir` は teardown で必ず復元されるため、その理由に抵触しない
- 移動先は `tmp_path` 配下（罠 T-3 を守る）。逸脱であることが docstring に明記されている
- `-C` を渡さない挙動（上方探索が**近い方**を掴む）は、`-C` を使う限り検証できない。**この1ケースだけ手段が無い**
- 残る唯一のリスクは、将来テストを並列実行した場合にプロセス全体の cwd が共有されること（RV-7 (d) に記録）

### E-11 新しく定義した語（完了条件7）

**申告すべき新語は無い。** 実装が導入した識別子（`TaskRunResult` / `ScaffoldResult` / `ScaffoldEntry` /
`Migration` / `SecretMaskingFilter` / `NetworkAccessBlockedError`）は、いずれも用語集に既にある概念
（AgentRunner / スキャフォールド / スキーマ版数 / 監査記録 / ネットワーク遮断）への型名であり、新しい意味を持たない。

境界にあるのは **`AuditRecord`** 1件のみ。「監査記録の1件」を表す型で、JSONL の9キー契約と
`schema_version` がこの型に載っている。用語集の `AuditRecorder` の項が書き込み口だけを説明しているため、
**登録するかどうかはオーケストレーターの判断**とする（レビューとしては必須と考えない）。
