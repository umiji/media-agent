# T-006: Agent Runtime・Task・Policy実装

## メタデータ
- 作成日: 2026-08-23
- 更新日: 2026-08-24
- 状態: 実装中
- 優先度: 高
- 担当: org-implementation
- 進捗: 1回目の担当が API のスペンド上限で中断。途中までの実装を dfa31fa で確保済み。再開待ち
- 依存: T-005（ブロッカー）— Task の永続化と Audit への記録が無いと、Runtime が要件を満たせないため
- TDD適用可否: 適用（T-003 の受け入れテスト S-E / S-F / S-G を通すことが目標）
- 手戻り回数: 0

## 目的

Media Agent の中核である「Agent を実行し、Task として管理し、Action の前に Policy で制御する」構造を作る。

要件定義書2.4節「DecisionとActionを分離」と開発原則5・8の実体がここである。
**Stage 0 の時点で Action の実行先（X 等）は存在しないが、Policy Check の関門を先に作っておくことに意味がある。**
後から差し込む構造にすると、Action を実装した時点で関門を通らない経路が残る。

## 変更範囲

- Agent Interface（要件定義書5.1節）
  - Agent 登録 / 実行 / Input 渡し / Output 取得 / 実行状態管理 / エラー処理 / 実行ログ
  - **検証用のダミー Agent**（T-002 で定義されたもの。実際の AI 呼び出しは行わない）。
    **正常系の `echo` と異常系の `fail` の2つを同梱する**（T-002 の範囲追記。詳細設計 8章。
    異常系を製品コードなしで検証できるようにするため。受け入れテスト S-F の `failed` 遷移がこれを使う）
- Task（要件定義書5.2節）
  - 状態 `pending` / `running` / `completed` / `failed` / `cancelled` と、T-002 が定めた状態遷移の制御
- Policy Engine（要件定義書5.3節・13節）
  - Action ごとの `mode`（`auto` / `approval` / `disabled`）と上限値の判定
- 上記に対応する単体テスト
- 本ファイル `docs/tasks/T-006.md` の 決定ログ / 作業ログ / 証拠 / 進捗

**`doctor` / `status` / `run` の統合は T-007 の範囲。** このタスクは Runtime・Task・Policy を
ライブラリとして完成させるところまでを担当する。

## 完了条件

1. Agent を Runtime へ登録し、Input を渡して実行し、Output を取得できる（**受け入れテスト S-E**）
2. Agent の実行が Task として永続化され、**T-002 が定めた状態遷移どおりに遷移する**（**受け入れテスト S-F**）
   - 正常系: `pending` → `running` → `completed`
   - 異常系: Agent が例外を投げたとき `failed` になり、**例外がそのまま外へ漏れない**
   - T-002 が定義していない遷移が**拒否される**こと
3. Agent の実行が Audit へ記録される（T-005 の Audit を使う。**新しい記録機構を作らない**）
4. Policy Engine が、少なくとも次の4通りを設計どおりに判定する（**受け入れテスト S-G**）
   - 許可（`mode: auto`）
   - 承認が必要（`mode: approval`）
   - 禁止（`mode: disabled`）
   - 上限超過（1日最大投稿数などの上限に達している）
5. Policy の設定が `config.yaml` から読み込まれる（T-004 の Config を使う。**独自に設定を読まない**）
6. Agent がエラーで落ちたとき、要件定義書5.1節「Agentエラー処理」どおりに扱われ、Audit に `error` が残る
7. Custom Agent のロードは**設計のみで実装しない**（要件定義書19節の表で S0 は「設計」）。
   T-002 の設計が実装を要求している場合は、着手前に停止条件として報告する

### 全実装タスク共通の完了条件

上記に加えて、次を全て満たすこと。

- **自分が書いたコードに単体テストがある**（受け入れテストは T-003 の担当。単体テストは実装の担当）
- T-001 が定めたテスト実行コマンドとリントコマンドが**通る**
- **担当範囲の受け入れテストが通る。** 他タスクの範囲に依存して落ちているものは、
  どのテストがなぜ落ちているかを `## 証拠` に明記して残す（そのまま次タスクへ引き継がれる）
- 論理的なまとまりごとに commit し、**タスク完了時に `claude/mvp01-implementation-testing-0ldwtq` へ push している**
  （レビュー通過を待たない。`CLAUDE.md` の Git 規約）
- `## 証拠` に、テスト実行コマンドと**実出力**、コミット SHA が記録されている

### T-003 からの申し送り（2026-08-23・オーケストレーター追記）

受け入れテストは **`tests/acceptance/` に既に存在し、全件落ちている**（コミット `7277a27`）。
**テストを書き換えて通してはならない**（禁止事項）。したがって、T-003 が設計の記述だけでは決まらず
**自分で決めた次の期待値は、実装側が合わせるべき契約**である。設計と矛盾すると判断した場合は、
直さずに停止条件として報告すること。

| # | 契約 | 根拠（T-003 の判断） |
| --- | --- | --- |
| K-1 | `media_agent.core.task` / `core.runtime` / `core.policy` / `agents.builtin` の主な公開物を、**`__init__.py` で再エクスポートする**（`from media_agent.core.task import TaskService` の形で import できること） | 詳細設計4章が「モジュール＝ディレクトリ、主な公開物＝その型」と対応付けている |
| K-2 | Repository のコンストラクタは `TaskRepository(conn)` / `DecisionRepository(conn)`（**位置引数**で `sqlite3.Connection`） | 詳細設計 6.6「すべて `sqlite3.Connection` を受け取り」。`__init__` の明示が無い |
| K-3 | `run --json` は**失敗時（終了コード1）にも** stdout へ JSON を出す | 15.3 の JSON に `error` キーがある以上、失敗時に出ないと `error` が無意味 |
| K-4 | 未初期化時の `doctor` / `status` / `run` / `agent list` / `task list` は、**stdout が空**（エラーは stderr） | 19章「stdout に成功を示す出力を出さない」 |
| K-5 | `run` の結果は**別プロセス（別接続）からも見える**。すなわちコミットが要る | 14章の出力例が実行済み Task の集計を示している |
| K-6 | `doctor --json` の `project_root` は `resolve()` 後のパスと比較できること | 絶対パスとしか書かれていないため、symlink 差で落ちないようにした |

### T-004 からの接続点（2026-08-24・オーケストレーター追記）

| # | 内容 |
| --- | --- |
| J-3 | **K-1（`__init__.py` での再エクスポート）のうち、`core.task` / `core.runtime` / `core.policy` / `agents.builtin` は本タスクの担当**である。`core.config` は T-004 で対応済み |
| J-5 | **`pytest`（引数なし）は本タスクが完了して初めて通る見込みである。** T-006 のモジュールを import する受け入れテスト3ファイルが、それまで収集時 `ModuleNotFoundError` になる。**`--continue-on-collection-errors` を既定に足さないこと**（本物の収集エラーを隠す） |
| J-6 | ruff の設定で `docs/` は対象外。**`ruff format` が Markdown 中の Python コードブロックを書き換えるため**。この除外を外さないこと |
| J-7 | `agent list` の本体（Registry の一覧）が未実装のため、受け入れテスト `test_agent_list_works_but_task_list_fails_on_broken_config` が落ちている。**Registry は本タスク、CLI 出力は T-007** |

### T-005 からの接続点（2026-08-24・オーケストレーター追記）

T-005 は完了済み（コミット `6deb006`）。DB 層・Migration・`core/clock.py`・Logger・`AuditRecorder` は実装されている。

| # | 内容 |
| --- | --- |
| L-1 | **`decisions.task_id` は `tasks` への外部キーで、実際に有効である。** `record_agent_run` は **Task 行を作った後・同じ接続で**呼ばないと `IntegrityError` になる（T-005 が実地で踏んだ）。`AgentRunner` の呼び出し順に効く |
| L-2 | **`DecisionRepository.add` は接続をコミットする**（6.6 の「境界は呼び出し側」の唯一の例外。11.3 手順1 が明示的にコミットを要求するため）。`with conn:` の途中で監査を記録すると、その時点の未コミット作業（Task の状態更新）も一緒に確定する。K-5 には好都合だが、意図しない部分コミットを避けたい箇所があれば記録の位置で調整すること |
| L-3 | **`TaskRepository.update_status` は遷移の可否を判定しない**（罠 D-T13）。判定は `TaskService.transition` の責務である。`update_status` の呼び出しは `TaskService` からのみに閉じること |
| L-4 | `AuditRecorder.record_policy_check` の引数は **Protocol**（`PolicyRequestLike` / `PolicyDecisionLike`）で受ける。`core.policy` が T-005 の時点に存在しなかったため。**詳細設計 10.2 の属性をそのまま持てば、無変更で渡せる** |
| L-5 | `DecisionRepository.count_allowed_actions(action=None, ...)` が全 Action 合計（`limits.max_actions_per_hour` 用）、`action="post"` が個別上限用（詳細設計 10.5 / D-X1） |
| L-6 | `open_project_db(layout, config)` は `projects` を同期してコミットする。`core/` が `project/` へ静的依存しないよう、引数は `ProjectPaths` Protocol（`db_path` のみ）で受ける。`ProjectLayout` は構造的に満たすので呼び出しは設計どおり |

## 判断してよい範囲

- Agent Runtime の内部実装（登録テーブルの持ち方、実行の同期/非同期）
- 例外階層の設計と命名
- モジュール分割・命名
- 単体テストの粒度と構成
- ダミー Agent を複数用意すること（正常系用・失敗系用など）

## 禁止事項

- **受け入れテスト（T-003 が書いたテスト）を書き換えて通さない。** テストが誤っていると判断したら、
  直さずに停止条件として報告する。`.claude/rules/org-task-ledger.md`「やってはいけないこと」に該当する
- 設計成果物（`docs/design/` 配下）を書き換えない。読み方に迷ったら org-design へ問い合わせる（1往復まで）
- `docs/requirements-media-agent-v0.2.md` / `docs/task-list-media-agent.csv` / `docs/handover.md` /
  `docs/po-queue.md` / 他タスクの `docs/tasks/T-XXX.md` を書き換えない
- **X API クライアントを書かない。外部ネットワークへ送信するコードを書かない**（PO 制約 C-1・C-3、
  および要件定義書28節「Stage 0ではまだX APIやニュース取得等を実装しない」）
- **認証情報を必要とする実装をしない。** 認証情報をリポジトリへ置かない（PO 制約 C-2、要件定義書14.2節）
- **Stage 1 以降の機能を実装しない。** Content / Research / Strategy / Analytics / Engagement / Orchestrator の
  各 Agent、Google News / RSS / Web Connector、過去投稿 Import、重複チェック、AI 呼び出しは範囲外
- **本タスクの `変更範囲` の外にあるファイルを変更しない。** 他の実装タスクが担当する領域に不足を見つけても、
  その場で直さずに報告する（`CLAUDE.md`「スコープを勝手に広げない」）
- スコープ外のリファクタリング・警告修正をしない
- **AI（LLM）を呼び出す実装をしない。** Stage 0 では AI 処理の実行方式そのものが未決である（要件定義書24節10番）
- **Policy Check を経由しない Action 実行経路を作らない**（要件定義書2.4節・開発原則5）

## テスト方法

- 単体テスト: Agent 登録・実行、状態遷移の許可/拒否、Policy の各判定、例外処理
- 受け入れテスト: T-003 が書いた S-E / S-F / S-G を実行する
- 実環境確認: **要**。ダミー Agent を実際に実行し、Task と Audit の記録を目視確認する。実施者は担当自身
- 回帰: T-004 / T-005 の受け入れテスト（S-A / S-D / S-H / S-I）が引き続き通ること

## 停止条件

共通の停止条件に加えて:

- T-002 の Task 状態遷移の定義に、到達不能な状態または矛盾がある
- Policy Model の定義だけでは、受け入れテスト S-G の4通りを判定できない
- 実装のために AI 呼び出しまたは外部接続が必要になった

## 参照すべき成果物

| パス | 何が書いてあるか |
| --- | --- |
| `docs/requirements-media-agent-v0.2.md` | 要件定義書 v0.2 全文。**18節 Stage 0・19節の対応表・28節を必ず読むこと** |
| `docs/design/stage0-architecture.md` | T-001 の方式設計成果物（構造・技術選定・実行コマンド） |
| `docs/design/stage0-detail.md` | T-002 の詳細設計成果物（Config / DB / Agent Interface / Task / Policy / Logging・Audit） |
| `docs/handover.md` | 今回のゴール達成条件 A-1〜A-8、PO 制約 C-1〜C-4 |
| `CLAUDE.md` | AI開発組織の運営規約 |
| `.claude/rules/org-task-ledger.md` | 決定ログ / 作業ログ / 証拠 の書き方 |
| `.claude/rules/org-escalation.md` | 共通の停止条件とエスカレーション手順 |
| `docs/glossary.md` | この組織で意味が決まっている語 |

**PO 制約（今回のゴール全体に効く）**:

- C-1 X への実際の投稿を行わない
- C-2 認証情報は環境に置かれていない。取得しようとしない
- C-3 投稿処理は、ダミーの送信先か、記録するだけの実装に対して開発する
- C-4 実際の投稿を試す必要が出たら、作業を止めてオーケストレーターへ報告する

**Stage 0 の範囲では投稿処理そのものが存在しない。** 要件定義書28節が「Stage 0 ではまだ X API や
ニュース取得等を実装しない」と明記しているため、ダミー送信先すら作らない。

なお、`docs/design/` 配下の実際のファイル名は T-001 / T-002 の成果物に従うこと。上表は想定である。

## 成果物

（完了時に担当が記入する。ファイルパスとコミット SHA で書くこと）

## ブロッカー

無し（T-005 は 2026-08-24 に `完了`）。

**2026-08-24: 1回目の担当が API のスペンド上限で中断した。** 途中までの実装は `dfa31fa` にある。
組織側の停滞ではなく、実行環境の資源制約である。

### 再開する担当への申し送り

| # | 内容 |
| --- | --- |
| W-1 | **`dfa31fa` の差分は確認済みとして扱ってよい**（共通停止条件 S-3 の対象にしない）。ただし**動作確認・テスト・リント・型検査はいずれも未実施**であり、内容の正しさは保証されていない。必要なら書き直すこと |
| W-2 | 入っているもの: `core/task/{__init__,models,service}.py`（Task モデルと状態遷移）、`core/runtime/{agent,registry}.py`（Agent Interface と Registry） |
| W-3 | **入っていないもの**: `core/runtime/__init__.py`（再エクスポート＝契約 K-1）、`core/policy/`（空ディレクトリ）、`agents/builtin/`（空ディレクトリ）、`errors.py` への例外追加（`AgentNotFoundError` / `DuplicateAgentError` / `InvalidAgentError` を registry.py が import している）、単体テスト |
| W-4 | 完了条件・禁止事項・申し送り（K / J / L）は**一切変わっていない**。上の本文をそのまま読むこと |

## 決定ログ

### 2026-08-24 中断した実装を WIP として commit した
- 決定: 1回目の担当エージェントが API のスペンド上限で中断したため、**未コミットだった実装を
  `dfa31fa` として commit した**。動作確認・テスト・リント・型検査はいずれも未実施
- 却下案: 破棄して最初からやり直す → **コンテナは揮発性であり、セッションが切れると失われる。**
  設計と用語集を正しく参照した状態まで進んでおり、捨てる理由が無い
- 却下案: 未コミットのまま次の担当へ渡す → 共通停止条件 S-3（作業ツリーに未確認の差分がある）に
  当たり、次の担当が着手前に差し戻すことになる
- 付帯条件: **再開する担当は、この差分を確認済みのものとして扱ってよい**（S-3 の対象にしない）。
  ただし内容の正しさは保証していないので、必要なら書き直してよい
- 出典: セッション内の判断（オーケストレーター記入）。コミット `dfa31fa`

## 作業ログ

## 証拠
