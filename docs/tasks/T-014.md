# T-014: 構造破損時の例外処理とレビュー指摘の修正

## メタデータ
- 作成日: 2026-08-24
- 更新日: 2026-08-24
- 状態: 未着手
- 優先度: 高
- 担当: org-implementation
- 進捗: 未着手。T-013 の完了を待つ
- 依存: T-013（ブロッカー）— 通すべき受け入れテストが存在しないと、TDD の的が無いため
- TDD適用可否: 適用（T-013 が先に書いた受け入れテストを通すことが目標）
- 手戻り回数: 0

## 目的

T-009（レビュー）の指摘のうち、**製品コードの修正を要するものを片づける。**
ゴール達成条件 **A-5（Blocker / Major が残っていない）** を満たすための最後の実装タスクである。

## 変更範囲

- `src/media_agent/core/db/connection.py`（例外の包み込み）
- `src/media_agent/project/scaffold.py`（同上）
- `src/media_agent/cli/commands/task.py`（`--limit` の値域検査）
- `src/media_agent/project/templates/config.yaml`（コメントの参照先）
- T-012 が D-2 で「Stage 0 で永続化する」と決めた場合は、Migration と Repository と `AuditRecorder`
- 上記に対応する単体テスト
- 本ファイル `docs/tasks/T-014.md` の 決定ログ / 作業ログ / 証拠 / 進捗

## 完了条件

1. **RV-1（Major）**: T-012 の D-1 が定めた期待値どおりに、構造が壊れたプロジェクトでの
   `init` / `doctor` / `status` / `run` / `agent list` / `task list` が振る舞う。
   **`doctor` の助言に従った利用者が行き止まりに入らないこと**（T-013 の受け入れテストで判定）
2. **RV-6**: `task list --limit` の値域が T-012 の D-3 どおりに検査される
3. **RV-5**: 生成される `config.yaml` のコメントが、**利用者のプロジェクトから到達できる参照先**を指す
   （T-010 が作った利用者向け文書を参照してよい。T-010 の成果物を確認すること）
4. **D-2 の決定に従っている**（永続化するなら実装し、送るなら**何もしない**。送ると決まったものを気を利かせて実装しない）
5. **T-013 が書いた受け入れテストが全て通る**
6. **既存の 538 件に回帰が無い**
7. `ruff check .` / `ruff format --check .` / `mypy src` の終了コードが 0
8. `claude/session-resume-mdqrmg` へ push している

## 判断してよい範囲

- 例外の包み方の実装手段（どこで捕まえ、どの型へ変換するか。**型そのものは T-012 が決める**）
- 単体テストの粒度と構成
- 命名・ファイル分割

## 禁止事項

- **受け入れテスト（T-013 / T-003 が書いたもの）を書き換えて通さない。** 誤っていると判断したら
  直さずに停止条件として報告する
- **設計成果物（`docs/design/` 配下）を書き換えない。** 読み方に迷ったら org-design へ問い合わせる（1往復まで）
- **T-009 が Minor と判定したもののうち、本ファイルの `変更範囲` に無いものへ手を出さない。**
  とくに **RV-2（`run --agent fail` の stderr トレースバック）は「このまま完了してよい」と判定済みである。直さない**
- **RV-7（Stage 1 以降への示唆）へ手を出さない**
- **F-2（受け入れテストの DB 接続リーク）へ手を出さない。** T-011 の範囲であり、担当は org-test
- **X API クライアントを書かない。外部ネットワークへ送信するコードを書かない**（PO 制約 C-1・C-3）
- **認証情報を必要とする実装をしない**（PO 制約 C-2）
- **Stage 1 以降の機能を実装しない**
- スコープ外のリファクタリング・警告修正をしない
- `docs/task-list-media-agent.csv` / `docs/handover.md` / `docs/po-queue.md` / 他タスクの `T-XXX.md` を書き換えない

## テスト方法

- 単体テスト: 例外の変換、`--limit` の値域
- 受け入れテスト: T-013 が書いたもの
- 実環境確認: **要**。構造を実際に壊したプロジェクトで各コマンドを実行し、終了コードとメッセージを目視する。実施者は担当自身
- 回帰: 既存 538 件

## 停止条件

共通の停止条件に加えて:

- T-012 の期待値どおりに実装すると、既存の受け入れテストが落ちる（**設計間の矛盾を示す**）
- D-2 の永続化が既存 DB の Migration を伴い、**S-2** に該当すると判明した

## 参照すべき成果物

| パス | 何が書いてあるか |
| --- | --- |
| `docs/tasks/T-009.md` の指摘一覧 | RV-1 / RV-5 / RV-6 の詳細。**対象ファイルと行番号が入っている** |
| `docs/tasks/T-012.md` の `## 決定ログ` | 従うべき期待値 |
| `docs/design/stage0-detail.md` 17.2 | T-012 が埋めた一覧 |
| `docs/design/stage0-architecture.md` 6.5 | 例外階層と、CLI 層を唯一の変換点にする方針 |
| `.claude/rules/quality-standards.md` | 品質基準 Q1〜Q10。**とくに Q8** |

## 成果物

（完了時に担当が記入する。ファイルパスとコミット SHA で書くこと）

## ブロッカー

T-013 が `完了` になるまで着手しない。

## 決定ログ

## 作業ログ

## 証拠
