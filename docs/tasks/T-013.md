# T-013: T-012の期待値を受け入れテストへ変換

## メタデータ
- 作成日: 2026-08-24
- 更新日: 2026-08-25
- 状態: 完了
- 優先度: 高
- 担当: org-test
- 進捗: S-D2〜S-D7 / S-J / S-K を受け入れテスト79件へ変換した。49件が「未実装」で落ち、30件は既に満たされている
- 依存: T-012（ブロッカー）— 期待値が確定していないとテストが書けないため。**この順序を崩すと、テストが独自解釈で期待値を作り、誰にも照合されないまま契約になる**（`docs/handover.md`「TDD の構造上の弱点への対処」）
- TDD適用可否: 適用（実装より先に書き、この時点では落ちるのが正しい）
- 手戻り回数: 0

## 目的

T-012 が確定させた期待値を、**機械で判定できる形**にする。T-014（実装）が通すべき的を先に置く。

## 変更範囲

- `tests/acceptance/` への追加（既存ファイルへの追加でも、新規シナリオでもよい）
- 本ファイル `docs/tasks/T-013.md` の 決定ログ / 作業ログ / 証拠 / 進捗

## 完了条件

1. **T-012 が確定させた D-1 / D-2 / D-3 の期待値が、すべて受け入れテストとして存在する**
2. **新しく書いたテストが落ちている。落ちた理由が「未実装」であり、期待値の不一致でないこと**
   （落ち方を `## 証拠` に記録する。**通ってしまうものがあれば、それは既に満たされているということなので、その旨を明記する**）
3. **既存の 538 件に回帰が無い**（新規追加分を除いて件数と結果が変わらないこと）
4. `ruff check .` / `ruff format --check .` の終了コードが 0
5. `claude/session-resume-mdqrmg` へ push している

### T-012 からの接続点（2026-08-25・オーケストレーター追記）

T-012 は完了済み（コミット `4f3ee23`）。**期待値は `docs/design/stage0-detail.md` の 17.4（構造破損）/
16.2（`--limit` の値域）/ 19.1（T-013 への引き渡し表）に確定している。19.1 が本タスクの的そのものである。**

| # | 内容 |
| --- | --- |
| Q-1 | 19.1 に **S-D2〜S-D7 / S-J / S-K** の引き渡し表がある。**そこに書かれた粒度で書けば、期待値を自分で作る必要は無い** |
| Q-2 | **S-D5（復旧経路が行き止まりでないこと）が RV-1 の回帰テスト本体である。** `doctor`=5 → 退避 → `init`=0 → `doctor`=0 の4手順。**これを落とさないこと** |
| Q-3 | 判定の要点は「**1 であって 70 でないこと**」。70 が返る限り RV-1 は解消していない |
| Q-4 | 壊し方は**ファイルの置き換え**で作ること（`chmod 000` は root 実行で効かず環境依存になる。19.1 の注記） |
| Q-5 | **`agents/`・`memory/` の破損では実行系が 0 を返す**（各コマンドは自分が必要とする構造だけを検査する = R-4）。**これも期待値である。「検査していないから通った」と読み替えないこと** |
| Q-6 | 異常が重なった場合の優先順位は R-7（未初期化 3 > 構造破損 1 > 設定 4 > 値域 2） |

#### 既存の受け入れテスト2件の期待値を、あなたが更新すること（重要）

**D-2（`Agent.version` の永続化）により `AUDIT_SCHEMA_VERSION` が 1 → 2、Migration 版数が 1 → 2 になる。**
そのため既存の受け入れテスト2件のリテラルが古くなる。

- `tests/acceptance/test_stage0_audit.py:64` — `record["schema_version"] == 1`
- `tests/acceptance/test_stage0_lifecycle_sequence.py:104` — `payload["database"]["schema_version"] == 1`

**これは「落ちているテストを通すための書き換え」ではなく、設計変更に伴う期待値の更新である。**
**更新する主体はあなた（org-test）であり、T-014（実装）は禁じられている。** 本タスクで直しておくこと。
`tests/acceptance/test_stage0_status.py:68` は定数比較のため修正不要（T-012 が確認済み）。

**この2件だけが対象である。** 他のテストの期待値には触れないこと。

## 判断してよい範囲

- テストの粒度・配置・命名
- 構造が壊れたプロジェクトの作り方（fixture の手段）

## 禁止事項

- **製品コード（`src/` 配下）を変更しない**
- **設計成果物（`docs/design/` 配下）を書き換えない。** T-012 の期待値に疑義があれば、
  直さずに停止条件として報告する（org-design へは1往復まで問い合わせてよい）
- **T-012 が定めていない期待値を自分で作らない。** 決まっていないものがあれば報告する。
  **これが T-003 で起きた「独自解釈が契約になる」問題の再発防止である**
- 既存のテストを書き換えない
- **ネットワークへ出るテスト・認証情報を要求するテストを書かない**（PO 制約 C-1・C-2）
- `docs/task-list-media-agent.csv` / `docs/handover.md` / `docs/po-queue.md` / 他タスクの `T-XXX.md` を書き換えない
- **`os.chdir` を使わない**（方式設計 10.3）。やむを得ない場合は `monkeypatch.chdir` を使い、逸脱を docstring に明記する
  （S-J に前例がある。T-009 が妥当と判定済み）

## テスト方法

`PATH="$PWD/.venv/bin:$PATH" .venv/bin/pytest -q` で、**新規分が落ち、既存 538 件が通る**ことを確認する。

## 停止条件

共通の停止条件に加えて:

- T-012 の期待値が二値で判定できない、または相互に矛盾している
- 期待値どおりのテストを書くために製品コードの変更が必要になった

## 参照すべき成果物

| パス | 何が書いてあるか |
| --- | --- |
| `docs/tasks/T-012.md` の `## 決定ログ` | 確定した期待値そのもの |
| `docs/design/stage0-detail.md` 17.2 | T-012 が埋めた一覧 |
| `tests/acceptance/` の既存シナリオ | 書き方の前例。とくに `expectations.py` |
| `docs/tasks/T-003.md` | 受け入れテストの設計方針 |

## 成果物

| 成果物 | 内容 | コミット |
| --- | --- | --- |
| `tests/acceptance/expectations.py` | 安定文字列 `MSG_SQLITE` / `MSG_QUARANTINE` / `MSG_LIMIT_OPTION` / `MSG_STATUS_OPTION` を追加（17.3 の追加3行） | `c913b2d` |
| `tests/acceptance/test_stage0_structure_breakage.py`（新規・59件） | S-D2〜S-D7。構造破損 × コマンドの終了コード / メッセージ / `doctor` の hint / 復旧経路の4手順 / `init` の preflight / `run` が記録を残さないこと | `6065232` |
| `tests/acceptance/test_stage0_task_list_limit.py`（新規・12件） | 19.1 の S-J。`--limit` の値域（D-3） | `ebf12f5` |
| `tests/acceptance/test_stage0_agent_version.py`（新規・8件） | 19.1 の S-K。`agent_version` の永続化と版数 2（D-2） | `ebf12f5` |
| `tests/acceptance/test_stage0_audit.py:65` / `test_stage0_lifecycle_sequence.py:107` | 版数のリテラル `1` を `AUDIT_SCHEMA_VERSION` / `SCHEMA_VERSION` との比較へ更新（19.1 の指定） | `1ba86f2` |

## ブロッカー

無し（T-012 は 2026-08-25 に `完了`。期待値は詳細設計 17.4 / 16.2 / 19.1 に確定している）。

## 決定ログ

### 2026-08-25 19.1 の `S-J` / `S-K` は T-008 の同名と別物である。新しい記号を作らずに限定して呼ぶ

- 決定: 詳細設計 19.1 が `S-J`（`--limit` の値域）/ `S-K`（`agent_version` の永続化）と呼ぶシナリオは、
  **T-008 が先に使った `S-J`（プロジェクト分離）/ `S-K`（連続実行）と記号が衝突している。**
  テストファイルは別に置き、docstring の冒頭で「19.1 の S-J / S-K を指す」と明記して区別した。
  **記号そのものは設計文書のまま使う**（`test_stage0_task_list_limit.py` / `test_stage0_agent_version.py`）
- 却下案:
  - **新しい記号（`S-L` / `S-M`）を自分で割り当てる** → 設計文書 19.1 との対応が取れなくなる。
    受け入れテストの記号は設計側の持ち物であり、テストが勝手に採番すると照合先が消える
  - **既存の `test_stage0_project_isolation.py` / `test_stage0_lifecycle_sequence.py` へ相乗りする** → 別の事実を判定する
    テストが1ファイルに混ざる。また「既存のテストを書き換えない」（本タスクの禁止事項）に触れる
  - **設計文書 19.1 の記号を直す** → `docs/design/` は本タスクの禁止事項。org-design の持ち物である
- 付帯条件: **記号の再割り当ては設計側の判断であり、オーケストレーターへ報告する**（本タスクの報告事項）
- 出典: `docs/design/stage0-detail.md` 19.1 / `docs/tasks/T-008.md` 116-117 行

### 2026-08-25 版数のリテラル `2` は S-K の1本にだけ置き、他は定数と比較する

- 決定: `SCHEMA_VERSION == 2` / `AUDIT_SCHEMA_VERSION == 2` を判定するテストを
  `test_stage0_agent_version.py::test_schema_versions_are_bumped_to_two` の**1本だけ**に置き、
  他のテスト（`PRAGMA user_version` / `status --json` / `audit.jsonl` の `schema_version`）は**定数と比較する**
- 却下案:
  - **すべて定数比較にする**（19.1 の「版数を上げるたびにテストを直さずに済む形」を字義どおり全件へ適用） →
    **D-2 が未実装でも全件が通ってしまい、版数 1 → 2 を誰も判定しない。** 受け入れテストが完了条件を検証できない
  - **すべてリテラル `2` と比較する** → 版数を上げるたびに複数のテストを直すことになる（19.1 が明示的に否定した形）
- 出典: `docs/design/stage0-detail.md` 19.1 / 6.4 / 11.3

## 作業ログ

- 2026-08-25: 着手前に `git status`（差分なし）/ HEAD `bd3a094` / ブランチ `claude/session-resume-mdqrmg` を確認。
  T-013.md 全文・T-012 の決定ログ・詳細設計 6.4 / 11.3 / 13.2 / 16.2 / 17.2 / 17.3 / 17.4 / 19.1、
  既存の `tests/acceptance/` の書き方（`expectations.py` / `conftest.py`）を読み、指示の充足を確認した（差し戻し事由なし）
- 2026-08-25: 基準値を実測（`538 passed`）。19.1 の引き渡し表どおりに S-D2〜S-D7 / S-J / S-K を79件へ変換した
- 2026-08-25: 壊し方は**ファイルの置き換え**で作った（19.1 の注記。`chmod 000` は root 実行で効かず環境依存になる）。
  退避は `Path.rename` で一時ディレクトリの外へ移す（`_quarantine`）。**製品側が退避することは無い**（R-2）
- 2026-08-25: 既存2件の版数リテラルを定数比較へ更新した（19.1 の指定。この2件だけが対象）
- 2026-08-25: `49 failed, 568 passed`。落ち方が全件「未実装」であることを確認し `## 証拠` へ記録した。
  コミット `c913b2d` / `6065232` / `ebf12f5` / `1ba86f2` を `claude/session-resume-mdqrmg` へ push した

## 証拠

### E-1 完了条件1: D-1 / D-2 / D-3 の期待値が受け入れテストとして存在する

19.1 の引き渡し表の8シナリオを全件変換した（**新規79件**）。

| 19.1 の ID | 由来 | ファイル | 件数 | 落 / 通 |
| --- | --- | --- | --- | --- |
| S-D2（終了コード） | D-1 | `test_stage0_structure_breakage.py` | 35 | 14 / 21 |
| S-D3（メッセージ） | D-1 | 同上 | 12 | 12 / 0 |
| S-D4（doctor の hint） | D-1 | 同上 | 3 | 3 / 0 |
| S-D5（復旧経路の4手順） | D-1 | 同上 | 6 | 6 / 0 |
| S-D6（init の preflight） | D-1 | 同上 | 2 | 2 / 0 |
| S-D7（run が記録を残さない） | D-1 | 同上 | 1 | 1 / 0 |
| S-J（`--limit` の値域） | D-3 | `test_stage0_task_list_limit.py` | 12 | 6 / 6 |
| S-K（`agent_version` の永続化） | D-2 | `test_stage0_agent_version.py` | 8 | 5 / 3 |

S-D2 の内訳: 構造破損3種 × コマンド6種 = 18 / `agents`・`memory` の破損2種 × コマンド6種 = 12 /
`.media-agent/` 自体が通常ファイル × コマンド5種 = 5。

### E-2 完了条件2: 新規テストが「未実装」で落ちている

```
$ PATH="$PWD/.venv/bin:$PATH" .venv/bin/pytest -q
49 failed, 568 passed in 14.48s
```

落ち方の内訳（**すべて「未実装」であり、期待値の不一致ではない**）:

| 落ち方 | 件数 | 読み |
| --- | --- | --- |
| `assert 70 == 1` | 32 | `sqlite3.Error` / `OSError` が CLI 層まで素通りしている。**R-1 が未実装**（T-009 が RV-1 として実測した状態そのもの） |
| `Error: 内部エラーが発生しました: [Errno 17] File exists: .../logs`（等） | 9 | `init` の preflight が無く、途中まで書いてから 70 で落ちる。**R-5 が未実装** |
| `assert '退避' in 'media-agent init を実行してください（既存ファイルは変更されません）'` | 6 | `doctor` の hint が構造破損でも文言を変えていない。**R-6 が未実装**（助言に従うと行き止まりになる） |
| `assert 0 == 2` ＋ stdout に一覧が出る | 6 | `--limit` の下限検査が無い。**D-3 が未実装**（`--limit 0` で `(タスクはありません)` が出る RV-6 の実測） |
| `no such column: agent_version` / `KeyError: 'agent_version'` / `assert 1 == 2` | 5 | 列・キー・版数が無い。**D-2 が未実装** |

### E-3 完了条件2: 通ってしまったもの（= 既に満たされている期待値）— 30件

**設計の期待値どおりであり、修正は不要。** T-014 は、これらを**壊さない**ことが要求される。

| 通っている期待値 | 件数 | 出所 |
| --- | --- | --- |
| 構造破損時の `doctor` = 5 / `agent list` = 0 | 6 | 17.2 の下から2〜4行目 |
| `agents/` `memory/` の破損で `doctor`=5・`status`/`run`/`agent list`/`task list`=0 | 10 | 17.2 の最終行 / R-4 |
| `.media-agent/` 自体が通常ファイル → 5コマンドすべて 3 | 5 | 17.2 の5行目 / 17.4.1 |
| `--limit 1` = 1行 / 上限なし / 未初期化が先に 3 / 非整数は Click が 2 / `--status` を先に報告 | 6 | 16.2 |
| `PRAGMA user_version` == `SCHEMA_VERSION` / `audit.jsonl` の `schema_version` == `AUDIT_SCHEMA_VERSION` / `doctor` の `db.schema` = ok | 3 | 6.4 / 7.1 / 11.3（**定数比較のため版数が 2 になっても通り続ける**） |

### E-4 完了条件3: 既存 538 件に回帰が無い

着手時 `538 passed` → 現在 `568 passed`。**差分 30 は E-3 の「新規追加分のうち通ったもの」と一致する**
（79 新規 = 49 failed + 30 passed）。既存テストの結果は1件も変わっていない。
19.1 が指定した既存2件も通っている（`test_stage0_audit.py` / `test_stage0_lifecycle_sequence.py` = `13 passed`）。

### E-5 完了条件4: lint / format

```
$ .venv/bin/ruff check .
All checks passed!
$ .venv/bin/ruff format --check .
100 files already formatted        # 終了コード 0
```

### E-6 完了条件5: push

`claude/session-resume-mdqrmg` へ push 済み（`c913b2d` / `6065232` / `ebf12f5` / `1ba86f2`）。

### E-7 T-012 の期待値で二値に判定できなかった箇所

**無し。** 19.1 の8シナリオはすべて二値のテストへ変換できた。ただし2点を報告する（判定不能ではない）:

1. **記号 `S-J` / `S-K` が T-008 と衝突している**（決定ログ参照）。**期待値の曖昧さではなく、識別子の重複である**
2. S-D3 の対象コマンドが 19.1 に明記されていないため、**終了コード 1 で終わる4コマンド**（`init` / `status` / `run` / `task list`）へ
   適用した。根拠は R-3（「構造破損を検出したコマンドはメッセージに絶対パスを含む」）であり、推測ではない
